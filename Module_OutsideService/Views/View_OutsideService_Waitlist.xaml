<Page
    x:Class="MTM_Receiving_Application.Module_OutsideService.Views.View_OutsideService_Waitlist"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MTM_Receiving_Application.Module_OutsideService.Models"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
        <converters:Converter_InverseBool x:Key="InverseBoolConverter" />
        <converters:Converter_InfoBarSeverity x:Key="InfoBarSeverityConverter" />
    </Page.Resources>

    <Grid RowSpacing="16" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#fbfcfe" BorderBrush="#d6dde8" BorderThickness="1" CornerRadius="18" Padding="16">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Header="Search" PlaceholderText="Search request, part, vendor, or BOL" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
                <ComboBox Grid.Column="1" Header="Phase" ItemsSource="{x:Bind ViewModel.PhaseFilterOptions, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedPhaseFilter, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <ComboBox Grid.Column="2" Header="Sort" ItemsSource="{x:Bind ViewModel.SortOptions, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedSortOption, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <Button Grid.Column="3" Content="Refresh" Command="{x:Bind ViewModel.RefreshCommand}" IsEnabled="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}" VerticalAlignment="Bottom" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnSpacing="16" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#fbfcfe" BorderBrush="#d6dde8" BorderThickness="1" CornerRadius="18" Padding="16">
                <ListView ItemsSource="{x:Bind ViewModel.FilteredLines, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedLine, Mode=TwoWay}" HorizontalAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_OutsideServiceRequestLine">
                            <Border Margin="0,0,0,8" Background="#ffffff" BorderBrush="#d6dde8" BorderThickness="1" CornerRadius="14" Padding="12">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind QueueKey}" FontFamily="Consolas" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind PartId}" FontFamily="Consolas" VerticalAlignment="Center" />
                                    <Border Grid.Column="2" Background="#e8f2ff" CornerRadius="12" Padding="10,4" Visibility="{x:Bind IsInitialize, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Initialize" Foreground="#0f6cbd" FontWeight="SemiBold" />
                                    </Border>
                                    <Border Grid.Column="2" Background="#fff4ce" CornerRadius="12" Padding="10,4" Visibility="{x:Bind IsSetup, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Setup" Foreground="#8a5d00" FontWeight="SemiBold" />
                                    </Border>
                                    <TextBlock Grid.Column="3" Text="{x:Bind PackageCountText}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="4" Text="{x:Bind WaitTimeLabel}" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <Grid Grid.Column="1" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Background="#f6f9ff" BorderBrush="#cfe1f7" BorderThickness="1" CornerRadius="18" Padding="16">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Line Detail" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock Text="{x:Bind ViewModel.SelectedLine.QueueKey, Mode=OneWay}" FontFamily="Consolas" Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Text="{x:Bind ViewModel.SelectedLine.PartId, Mode=OneWay}" FontFamily="Consolas" Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Text="{x:Bind ViewModel.SelectedLine.CreatedByDisplay, Mode=OneWay}" Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Text="{x:Bind ViewModel.SelectedLine.PackageSummaryMultiline, Mode=OneWay}" FontFamily="Consolas" TextWrapping="Wrap" Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Text="Select a line to review it and open the next step." Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="#f6f9ff" BorderBrush="#cfe1f7" BorderThickness="1" CornerRadius="18" Padding="16">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Actions" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock Text="Open the selected line to continue or complete setup." TextWrapping="Wrap" />
                        <Button Content="{x:Bind ViewModel.PrimaryActionText, Mode=OneWay}" Command="{x:Bind ViewModel.OpenSelectedLineCommand}" Style="{StaticResource AccentButtonStyle}" Visibility="{x:Bind ViewModel.HasSelectedLine, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                </Border>

                <InfoBar
                    Grid.Row="2"
                    IsOpen="{x:Bind ViewModel.IsStatusOpen, Mode=TwoWay}"
                    Severity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay, Converter={StaticResource InfoBarSeverityConverter}}"
                    Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                    IsClosable="True" />
            </Grid>
        </Grid>
    </Grid>
</Page>