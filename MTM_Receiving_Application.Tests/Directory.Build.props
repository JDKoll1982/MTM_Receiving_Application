<Project>
    <PropertyGroup>
        <!-- Ensure tests always build with x64 platform (required for WinUI 3) -->
        <Platform Condition="'$(Platform)' == '' OR '$(Platform)' == 'AnyCPU'">x64</Platform>
    </PropertyGroup>
</Project>
