<Page
    x:Class="MTM_Receiving_Application.Module_Scanner.Views.View_Scanner_AdvancedBulkMove"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:models="using:MTM_Receiving_Application.Module_Scanner.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
        <converters:Converter_InverseBooleanToVisibility x:Key="InverseBooleanToVisibilityConverter" />
        <converters:Converter_StringFormat x:Key="StringFormatConverter" />
        <Style x:Key="ScannerSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style x:Key="ScannerActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>
        <Style x:Key="ScannerPrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="104" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>
    </Page.Resources>

    <Grid
        x:Name="AdvancedBulkMoveRootGrid"
        Margin="4,0,4,4"
        RowSpacing="14"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Step header  -->
        <TextBlock
            Grid.Row="0"
            Style="{StaticResource ScannerSectionHeaderStyle}"
            Text="{x:Bind ViewModel.StepHeaderText, Mode=OneWay}"
            TextWrapping="Wrap" />

        <!--  Search row (Step 1 only)  -->
        <Grid
            x:Name="SearchPanel"
            Grid.Row="1"
            ColumnSpacing="10"
            Visibility="{x:Bind ViewModel.IsStepTwo, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AutoSuggestBox
                x:Name="FromLocationSearchBox"
                Grid.Column="0"
                VerticalAlignment="Bottom"
                PlaceholderText="Search a From Location (e.g. RECV)..."
                QueryIcon="Find"
                Text="{x:Bind ViewModel.FromLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                TextChanged="FromLocationSearchBox_TextChanged"
                SuggestionChosen="FromLocationSearchBox_SuggestionChosen"
                QuerySubmitted="FromLocationSearchBox_QuerySubmitted"
                ItemsSource="{x:Bind ViewModel.LocationSuggestions, Mode=OneWay}"
                DisplayMemberPath="Label" />

            <Button
                Grid.Column="1"
                VerticalAlignment="Bottom"
                Style="{StaticResource ScannerActionButtonStyle}"
                Command="{x:Bind ViewModel.SearchPartsCommand}"
                Content="Search" />
        </Grid>

        <!--  Step 1: parts list  -->
        <Grid
            x:Name="Step1Panel"
            Grid.Row="2"
            RowSpacing="8"
            Visibility="{x:Bind ViewModel.IsStepTwo, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="✓" />
                <TextBlock Grid.Column="1" Text="Entries" />
                <TextBlock Grid.Column="2" Text="Part" />
                <TextBlock Grid.Column="3" Text="Description" />
                <TextBlock Grid.Column="4" Text="Qty" />
            </Grid>

            <ListView
                x:Name="PartsListView"
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.Parts, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0,2" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Model_ScannerBulkMovePart">
                        <Grid ColumnSpacing="12" Padding="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <CheckBox
                                Grid.Column="0"
                                MinWidth="20"
                                VerticalAlignment="Center"
                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                            <NumberBox
                                Grid.Column="1"
                                Width="70"
                                Minimum="1"
                                Maximum="99"
                                SmallChange="1"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind EntryCount, Mode=TwoWay}" />
                            <TextBlock
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Text="{x:Bind PartId}" />
                            <TextBlock
                                Grid.Column="3"
                                VerticalAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                Text="{x:Bind PartDescription}" />
                            <TextBlock
                                Grid.Column="4"
                                VerticalAlignment="Center"
                                Text="{x:Bind Quantity, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:0.####}'}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--  Step 2: destinations list  -->
        <Grid
            x:Name="Step2Panel"
            Grid.Row="2"
            RowSpacing="8"
            Visibility="{x:Bind ViewModel.IsStepTwo, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Part" />
                <TextBlock Grid.Column="1" Text="From" />
                <TextBlock Grid.Column="2" Text="To Loc" />
                <TextBlock Grid.Column="3" Text="Qty" />
            </Grid>

            <ListView
                x:Name="DestinationsListView"
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.Destinations, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0,2" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Model_ScannerBulkMoveDestination">
                        <Grid ColumnSpacing="12" Padding="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Text="{x:Bind PartId}" />
                            <TextBlock
                                Grid.Column="1"
                                MinWidth="70"
                                VerticalAlignment="Center"
                                Text="{x:Bind FromLocation}" />
                            <TextBox
                                Grid.Column="2"
                                PlaceholderText="To Loc"
                                Text="{x:Bind ToLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <NumberBox
                                Grid.Column="3"
                                Minimum="0"
                                SmallChange="1"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind QuantityNumber, Mode=TwoWay}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--  Footer: status + actions  -->
        <Grid
            Grid.Row="3"
            ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                x:Name="StatusTextBlock"
                Grid.Column="0"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind ViewModel.InlineStatus, Mode=OneWay}"
                TextWrapping="Wrap" />

            <Button
                Grid.Column="1"
                Style="{StaticResource ScannerActionButtonStyle}"
                Command="{x:Bind ViewModel.BackToStepOneCommand}"
                Content="Back"
                Visibility="{x:Bind ViewModel.IsStepTwo, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <Button
                Grid.Column="2"
                Style="{StaticResource ScannerActionButtonStyle}"
                Command="{x:Bind ViewModel.CancelCommand}"
                Content="Cancel" />

            <Button
                Grid.Column="3"
                Style="{StaticResource ScannerPrimaryActionButtonStyle}"
                Command="{x:Bind ViewModel.NextStepCommand}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon
                        FontSize="14"
                        Glyph="{x:Bind ViewModel.NextButtonIcon, Mode=OneWay}" />
                    <TextBlock
                        VerticalAlignment="Center"
                        Text="{x:Bind ViewModel.NextButtonText, Mode=OneWay}" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
