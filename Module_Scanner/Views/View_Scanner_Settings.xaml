<Page
    x:Class="MTM_Receiving_Application.Module_Scanner.Views.View_Scanner_Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MTM_Receiving_Application.Module_Scanner.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="ScannerSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style x:Key="ScannerActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>
    </Page.Resources>

    <Grid Padding="16" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button
            Canvas.ZIndex="100"
            Width="34"
            Height="34"
            Margin="0,8,16,0"
            Padding="0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            AutomationProperties.Name="Help"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Click="OnHelpClick"
            CornerRadius="4"
            ToolTipService.ToolTip="Help">
            <FontIcon FontSize="16" Glyph="&#xE946;" />
        </Button>

        <!-- Profile list -->
        <Grid Grid.Column="0" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Style="{StaticResource ScannerSectionHeaderStyle}" Text="Profiles" />
            <ListView
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Model_ScannerProfile">
                        <StackPanel Spacing="2" Padding="4">
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind ProfileName}" />
                            <TextBlock Opacity="0.7" Text="{x:Bind TargetExecutableName}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button
                Grid.Row="1"
                VerticalAlignment="Bottom"
                Margin="0,8,0,0"
                Style="{StaticResource ScannerActionButtonStyle}"
                Command="{x:Bind ViewModel.LoadProfilesCommand}"
                Content="Reload" />
        </Grid>

        <!-- Editor -->
        <Grid Grid.Column="1" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{StaticResource ScannerSectionHeaderStyle}" Text="Profile Editor" />
            <TextBox Grid.Row="1" Header="Profile Name" Text="{x:Bind ViewModel.ProfileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="2" Header="Target Executable" Text="{x:Bind ViewModel.TargetExecutableName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="3" Header="App Window Title" Text="{x:Bind ViewModel.AppWindowTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="4" Header="Child Window Title" Text="{x:Bind ViewModel.TargetChildWindowTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="5" Header="Window Class" Text="{x:Bind ViewModel.AppWindowClass, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="6" Header="From Warehouse Default" Text="{x:Bind ViewModel.FromWarehouseDefault, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="7" Header="To Warehouse Default" Text="{x:Bind ViewModel.ToWarehouseDefault, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="8" Header="Send Shortcut Chord" Text="{x:Bind ViewModel.SendShortcutChord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <StackPanel Grid.Row="9" Orientation="Horizontal" Spacing="8">
                <Button Style="{StaticResource ScannerActionButtonStyle}" Command="{x:Bind ViewModel.SaveProfileCommand}" Content="Save" />
                <Button Style="{StaticResource ScannerActionButtonStyle}" Command="{x:Bind ViewModel.DeleteProfileCommand}" Content="Delete" />
                <Button Style="{StaticResource ScannerActionButtonStyle}" Command="{x:Bind ViewModel.SetDefaultProfileCommand}" Content="Set Default" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
