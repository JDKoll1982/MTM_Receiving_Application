<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MTM_Receiving_Application.Views.Shared.SplashScreenWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="MTM Receiving Application">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Logo and Branding -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Spacing="16">
            <FontIcon FontFamily="Segoe Fluent Icons" 
                      Glyph="&#xE8F1;" 
                      FontSize="72"
                      Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                      HorizontalAlignment="Center"/>
            
            <TextBlock Text="MTM Receiving Application" 
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       FontWeight="SemiBold"/>
            
            <TextBlock Text="Version 1.0.0"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"/>
            
            <TextBlock Text="MTM Manufacturing"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Progress Section -->
        <StackPanel Grid.Row="2" Margin="0,48,0,0" Spacing="12">
            <TextBlock x:Name="StatusMessageTextBlock"
                       Text="Initializing..."
                       Style="{StaticResource BodyTextBlockStyle}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>

            <ProgressBar x:Name="MainProgressBar"
                         Height="4"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         HorizontalAlignment="Stretch"/>

            <TextBlock x:Name="ProgressPercentageTextBlock"
                       Text="0%"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Copyright -->
        <TextBlock Grid.Row="3"
                   Text="© 2025 MTM Manufacturing. All rights reserved."
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,32,0,0"/>
    </Grid>
</Window>
