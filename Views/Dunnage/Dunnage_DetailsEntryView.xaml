<UserControl
    x:Class="MTM_Receiving_Application.Views.Dunnage.Dunnage_DetailsEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MTM_Receiving_Application.Views.Dunnage"
    xmlns:converters="using:MTM_Receiving_Application.Converters"
    mc:Ignorable="d"
    x:DataType="views:Dunnage_DetailsEntryView"
    Loaded="OnLoaded">

    <UserControl.Resources>
        <converters:Converter_IntToVisibility x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,24">
            <TextBlock Text="Enter Details"
                       Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Provide PO number, location, and specifications for this dunnage."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Style="{StaticResource BodyTextBlockStyle}"/>
        </StackPanel>

        <!-- Inventory Notification -->
        <InfoBar Grid.Row="1"
                 IsOpen="{x:Bind ViewModel.IsInventoryNotificationVisible, Mode=OneWay}"
                 Severity="Informational"
                 Title="Inventory Action Required"
                 Message="{x:Bind ViewModel.InventoryNotificationMessage, Mode=OneWay}"
                 IsClosable="False"
                 Margin="0,0,0,16"/>

        <!-- Details Form -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" MaxWidth="800" HorizontalAlignment="Stretch">
                
                <!-- PO Number and Location -->
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- PO Number -->
                    <StackPanel Grid.Column="0" Spacing="8">
                        <TextBlock Text="PO Number"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBox Text="{x:Bind ViewModel.PoNumber, Mode=TwoWay}"
                                 PlaceholderText="Enter PO number (optional)..."
                                 HorizontalAlignment="Stretch"/>
                        <TextBlock Text="Leave blank for non-PO dunnage"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>

                    <!-- Location -->
                    <StackPanel Grid.Column="1" Spacing="8">
                        <TextBlock Text="Location"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBox Text="{x:Bind ViewModel.Location, Mode=TwoWay}"
                                 PlaceholderText="Enter location..."
                                 HorizontalAlignment="Stretch"/>
                        <TextBlock Text="Where this dunnage is located"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </Grid>

                <!-- Dynamic Spec Inputs -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Specifications"
                               Style="{StaticResource SubtitleTextBlockStyle}"/>
                    
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.SpecInputs, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Spacing="8" Margin="0,0,0,16">
                                    <TextBlock Text="{Binding SpecName}"
                                               FontWeight="SemiBold"/>
                                    
                                    <!-- Text Input -->
                                    <TextBox Text="{Binding Value, Mode=TwoWay}"
                                             PlaceholderText="Enter value..."
                                             HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>

                    <!-- Placeholder when no specs -->
                    <TextBlock Text="No specifications configured for this type."
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Visibility="{x:Bind ViewModel.SpecInputs.Count, Mode=OneWay, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter='Inverse'}"
                               Style="{StaticResource BodyTextBlockStyle}"/>
                </StackPanel>

                <!-- Info Panel -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE946;" FontSize="16" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            <TextBlock Text="Note" FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="• PO Number: Use if receiving against a purchase order"/>
                            <LineBreak/>
                            <Run Text="• Location: Physical location of the dunnage"/>
                            <LineBreak/>
                            <Run Text="• Specifications: Additional details for identification"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Navigation Buttons -->
        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Command="{x:Bind ViewModel.GoBackCommand}"
                    HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72B;" FontSize="16"/>
                    <TextBlock Text="Back"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="1"
                    Command="{x:Bind ViewModel.GoNextCommand}"
                    Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Review"/>
                    <FontIcon Glyph="&#xE72A;" FontSize="16"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
