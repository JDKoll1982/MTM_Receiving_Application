<Page
    x:Class="MTM_Receiving_Application.Views.Dunnage.Dunnage_AdminTypesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="OnPageLoaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock 
                Text="Manage Dunnage Types" 
                Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock 
                Text="Add, edit, and delete dunnage types with impact analysis" 
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" Margin="0,0,0,16">
            <AppBarButton 
                Icon="Add" 
                Label="Add New Type"
                ToolTipService.ToolTip="Create a new dunnage type (coming soon)"/>
            <AppBarSeparator/>
            <AppBarButton 
                Icon="Edit" 
                Label="Edit Type"
                Command="{x:Bind ViewModel.ShowEditTypeCommand}"
                IsEnabled="{x:Bind ViewModel.CanEdit, Mode=OneWay}"
                ToolTipService.ToolTip="Edit selected type"/>
            <AppBarButton 
                Icon="Delete" 
                Label="Delete Type"
                Command="{x:Bind ViewModel.ShowDeleteConfirmationCommand}"
                IsEnabled="{x:Bind ViewModel.CanDelete, Mode=OneWay}"
                ToolTipService.ToolTip="Delete selected type with impact analysis"/>
            <AppBarSeparator/>
            <AppBarButton 
                Icon="Back" 
                Label="Back to Admin Hub"
                Command="{x:Bind ViewModel.ReturnToAdminHubCommand}"
                ToolTipService.ToolTip="Return to admin main navigation"/>
        </CommandBar>

        <!-- DataGrid -->
        <Border 
            Grid.Row="2" 
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="1">
            <Grid>
                <controls:DataGrid
                    ItemsSource="{x:Bind ViewModel.Types, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedType, Mode=TwoWay}"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    GridLinesVisibility="Horizontal"
                    HeadersVisibility="Column"
                    SelectionMode="Single"
                    CanUserReorderColumns="False"
                    CanUserSortColumns="True"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Auto"
                    AlternatingRowBackground="{ThemeResource SubtleFillColorSecondaryBrush}">

                    <controls:DataGrid.Columns>
                        <!-- Icon Column -->
                        <controls:DataGridTextColumn 
                            Header="Icon" 
                            Binding="{Binding Icon}"
                            Width="60"
                            FontSize="24"
                            CanUserResize="False"/>

                        <!-- Type Name Column -->
                        <controls:DataGridTextColumn 
                            Header="Type Name" 
                            Binding="{Binding DunnageType}"
                            Width="2*"
                            CanUserResize="True"/>

                        <!-- Date Added Column -->
                        <controls:DataGridTextColumn 
                            Header="Date Added" 
                            Binding="{Binding DateAdded, Converter={StaticResource DateTimeToStringConverter}}"
                            Width="*"
                            CanUserResize="True"/>

                        <!-- Added By Column -->
                        <controls:DataGridTextColumn 
                            Header="Added By" 
                            Binding="{Binding AddedBy}"
                            Width="*"
                            CanUserResize="True"/>

                        <!-- Last Modified Column -->
                        <controls:DataGridTextColumn 
                            Header="Last Modified" 
                            Binding="{Binding LastModified, Converter={StaticResource DateTimeToStringConverter}}"
                            Width="*"
                            CanUserResize="True"/>

                        <!-- Modified By Column -->
                        <controls:DataGridTextColumn 
                            Header="Modified By" 
                            Binding="{Binding ModifiedBy}"
                            Width="*"
                            CanUserResize="True"/>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>

                <!-- Empty State -->
                <StackPanel 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Spacing="16"
                    Visibility="{x:Bind ViewModel.Types.Count, Mode=OneWay, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=Inverse}">
                    <FontIcon 
                        Glyph="&#xE7C3;" 
                        FontSize="72"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBlock 
                        Text="No types found" 
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBlock 
                        Text="Click 'Add New Type' to create your first dunnage type"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="0,16,0,0">
            <ProgressRing 
                IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                Width="20" 
                Height="20"/>
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"/>
            <TextBlock 
                Text="|"
                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                Margin="8,0,8,0"/>
            <TextBlock>
                <Run Text="Total Types:"/>
                <Run Text="{x:Bind ViewModel.Types.Count, Mode=OneWay}" FontWeight="SemiBold"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
