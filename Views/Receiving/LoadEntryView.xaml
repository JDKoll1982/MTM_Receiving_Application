<UserControl
    x:Class="MTM_Receiving_Application.Views.Receiving.LoadEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button x:Name="HelpButton"
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="16"
                Padding="12,8"
                ToolTipService.ToolTip="Click for help about load entry">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE946;" FontSize="16"/>
                <TextBlock Text="Help"/>
            </StackPanel>
            <Button.Flyout>
                <Flyout>
                    <ScrollViewer MaxWidth="400" MaxHeight="600">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Number of Loads" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock TextWrapping="Wrap">How many skids/loads are you receiving for this part?</TextBlock>
                            
                            <TextBlock Text="Examples" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock TextWrapping="Wrap">Coils: Each Skid of Coils = 1 load. Sheets: Each skid of sheets = 1 load. Bars: Each bundle = 1 load.</TextBlock>
                            
                            <TextBlock Text="Valid Range" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock TextWrapping="Wrap">Enter between 1 and 99 loads.</TextBlock>
                            
                            <TextBlock Text="Note" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorCautionBrush}" Margin="0,8,0,4"/>
                            <TextBlock TextWrapping="Wrap">Each load will be labeled separately for tracking.</TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </Flyout>
            </Button.Flyout>
        </Button>

        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="32"
                MinWidth="500">
            <StackPanel Spacing="24" HorizontalAlignment="Center">
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE7B8;" 
                             FontSize="48"
                             Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                             HorizontalAlignment="Center"/>
                    <TextBlock Text="{x:Bind ViewModel.SelectedPartInfo, Mode=OneWay}" 
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"/>
                </StackPanel>

                <NumberBox Header="Number of Loads (1-99)"
                           Value="{x:Bind ViewModel.NumberOfLoads, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           Minimum="1"
                           Maximum="99"
                           SpinButtonPlacementMode="Inline"
                           Width="250"
                           HorizontalAlignment="Center"
                           AutomationProperties.Name="Number of Loads"/>
                       
                <TextBlock Text="Enter the total number of skids/loads for this part." 
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
