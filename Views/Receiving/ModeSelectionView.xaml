<UserControl
    x:Class="MTM_Receiving_Application.Views.Receiving.ModeSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MTM_Receiving_Application.ViewModels.Receiving"
    mc:Ignorable="d">

    <Grid>
        <Button x:Name="HelpButton"
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="16"
                Padding="12,8"
                ToolTipService.ToolTip="Click for help about entry mode selection">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE946;" FontSize="16"/>
                <TextBlock Text="Help"/>
            </StackPanel>
            <Button.Flyout>
                <Flyout>
                    <ScrollViewer MaxWidth="400" MaxHeight="600">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Entry Mode Selection" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock TextWrapping="Wrap">Choose how you want to enter receiving data.</TextBlock>
                            
                            <TextBlock Text="Guided Wizard" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock TextWrapping="Wrap">Step-by-step process for standard receiving. Ideal for one PO or part at a time. Validates data at each step. Best for beginners or infrequent users.</TextBlock>
                            
                            <TextBlock Text="Manual Entry" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock TextWrapping="Wrap">Grid-based bulk data entry. Enter multiple loads at once. Best for experienced users. Faster for large shipments.</TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </Flyout>
            </Button.Flyout>
        </Button>

        <StackPanel Spacing="32" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Spacing="24">
            <!-- Guided Mode Button -->
            <Button Command="{x:Bind ViewModel.SelectGuidedModeCommand}"
                    Width="280" Height="180"
                    Padding="20"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    AutomationProperties.Name="Guided Wizard Mode">
                <StackPanel Spacing="12">
                    <FontIcon Glyph="&#xE771;" FontSize="48" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Guided Wizard" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Step-by-step process for standard receiving workflow." 
                               Style="{StaticResource BodyTextBlockStyle}" 
                               TextWrapping="Wrap" 
                               TextAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Button>

            <!-- Manual Mode Button -->
            <Button Command="{x:Bind ViewModel.SelectManualModeCommand}"
                    Width="280" Height="180"
                    Padding="20"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    AutomationProperties.Name="Manual Entry Mode">
                <StackPanel Spacing="12">
                    <FontIcon Glyph="&#xE7F0;" FontSize="48" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Manual Entry" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Customizable grid for bulk data entry and editing." 
                               Style="{StaticResource BodyTextBlockStyle}" 
                               TextWrapping="Wrap" 
                               TextAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Button>
        </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
