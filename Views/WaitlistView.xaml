<UserControl x:Class="MTM_Waitlist_Application_2._0.Views.WaitlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MTM_Waitlist_Application_2._0.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="750">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="1" Background="White">
            <views:WaitlistComponentDetails DataContext="{Binding WaitlistComponentDetailsViewModel}"/>
        </Border>
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Background="Transparent">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid Grid.Column="0" x:Name="ListView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <views:WaitlistComponentList DataContext="{Binding WaitlistComponentListViewModel}"/>
                </Grid>
            </Grid>
        </Border>

    </Grid>



</UserControl>
