<Page
    x:Class="MTM_Receiving_Application.Module_Settings.Core.Views.View_Settings_Database"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:MTM_Receiving_Application.Module_Settings.Core.ViewModels">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Database" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Review connection defaults, stored procedures, and health indicators."
                       Style="{StaticResource BodyTextBlockStyle}" />
        </StackPanel>

        <InfoBar Grid.Row="1"
                 IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="Overview" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock Text="Database configuration is managed through Core Settings policies and stored procedure contracts."
                       Style="{StaticResource BodyTextBlockStyle}" />
        </StackPanel>
    </Grid>
</Page>
