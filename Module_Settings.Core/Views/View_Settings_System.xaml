<Page
    x:Class="MTM_Receiving_Application.Module_Settings.Core.Views.View_Settings_System"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MTM_Receiving_Application.Module_Settings.Core.Models"
    xmlns:viewmodels="using:MTM_Receiving_Application.Module_Settings.Core.ViewModels">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="System Settings" />
            <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="Core system defaults registered in the settings manifest." />
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="12">
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Registered Definitions" />
            <ScrollViewer Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}">
                <ItemsControl ItemsSource="{x:Bind ViewModel.Definitions, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_SettingsDefinition">
                            <StackPanel Margin="0,6,0,6" Spacing="2">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind DisplayName}" />
                                <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Key}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
