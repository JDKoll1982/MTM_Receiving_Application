<Window
    x:Class="MTM_Receiving_Application.Module_Settings.Core.Views.View_Settings_CoreWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:MTM_Receiving_Application.Module_Settings.Core.ViewModels"
    xmlns:views="using:MTM_Receiving_Application.Module_Settings.Core.Views"
    Title="Core Settings">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid x:Name="AppTitleBar"
              Height="48"
              VerticalAlignment="Top"
              Canvas.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12" Margin="16,0,0,0">
                <Image Source="Assets/Square44x44Logo.scale-200.png"
                       Width="16"
                       Height="16"
                       VerticalAlignment="Center" />
                <TextBlock Text="MTM Receiving Application"
                           VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Grid>

        <NavigationView x:Name="SettingsNavView"
                        PaneDisplayMode="Left"
                        IsSettingsVisible="False"
                        SelectionChanged="OnSelectionChanged">
            <NavigationView.PaneHeader>
                <TextBlock Text="Core Settings"
                           Style="{StaticResource TitleTextBlockStyle}"
                           Margin="12,0,0,0" />
            </NavigationView.PaneHeader>

            <NavigationView.Header>
                <Grid Margin="12,0,12,12" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="6">
                        <TextBlock x:Name="HeaderTitleText"
                                   Text="Configuration"
                                   Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock x:Name="HeaderDescriptionText"
                                   Text="Manage core system defaults, users, and infrastructure settings."
                                   Style="{StaticResource BodyTextBlockStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                        <Button x:Name="BackToHubButton"
                                Content="Back to Hub"
                                Click="OnBackToHubClicked"
                                Visibility="Collapsed" />
                    </StackPanel>
                </Grid>
            </NavigationView.Header>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Core Settings" Tag="CoreSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Receiving Navigation" Tag="ReceivingSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE74C;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Dunnage Navigation" Tag="DunnageSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7B8;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Routing Navigation" Tag="RoutingSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE724;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Reporting Navigation" Tag="ReportingSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9F9;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Volvo Navigation" Tag="VolvoSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7C1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Developer Tools" Tag="DeveloperToolsSettingsHub" Margin="0,0,0,4">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7FC;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="SettingsFrame" />
        </NavigationView>
    </Grid>
</Window>
