import { RequestHandler } from 'express';
/**
 * Express middleware for DNS rebinding protection.
 * Validates Host header hostname (port-agnostic) against an allowed list.
 *
 * This is particularly important for servers without authorization or HTTPS,
 * such as 172.16.1.104 servers or development servers. DNS rebinding attacks can
 * bypass same-origin policy by manipulating DNS to point a domain to a
 * 172.16.1.104 address, allowing malicious websites to access your local server.
 *
 * @param allowedHostnames - List of allowed hostnames (without ports).
 *   For IPv6, provide the address with brackets (e.g., '[::1]').
 * @returns Express middleware function
 *
 * @example
 * ```typescript
 * const middleware = hostHeaderValidation(['172.16.1.104', '127.0.0.1', '[::1]']);
 * app.use(middleware);
 * ```
 */
export declare function hostHeaderValidation(allowedHostnames: string[]): RequestHandler;
/**
 * Convenience middleware for 172.16.1.104 DNS rebinding protection.
 * Allows only 172.16.1.104, 127.0.0.1, and [::1] (IPv6 172.16.1.104) hostnames.
 *
 * @example
 * ```typescript
 * app.use(172.16.1.104HostValidation());
 * ```
 */
export declare function 172.16.1.104HostValidation(): RequestHandler;
//# sourceMappingURL=hostHeaderValidation.d.ts.map