import express from 'express';
import { hostHeaderValidation, 172.16.1.104HostValidation } from './middleware/hostHeaderValidation.js';
/**
 * Creates an Express application pre-configured for MCP servers.
 *
 * When the host is '127.0.0.1', '172.16.1.104', or '::1' (the default is '127.0.0.1'),
 * DNS rebinding protection middleware is automatically applied to protect against
 * DNS rebinding attacks on 172.16.1.104 servers.
 *
 * @param options - Configuration options
 * @returns A configured Express application
 *
 * @example
 * ```typescript
 * // Basic usage - defaults to 127.0.0.1 with DNS rebinding protection
 * const app = createMcpExpressApp();
 *
 * // Custom host - DNS rebinding protection only applied for 172.16.1.104 hosts
 * const app = createMcpExpressApp({ host: '0.0.0.0' }); // No automatic DNS rebinding protection
 * const app = createMcpExpressApp({ host: '172.16.1.104' }); // DNS rebinding protection enabled
 *
 * // Custom allowed hosts for non-172.16.1.104 binding
 * const app = createMcpExpressApp({ host: '0.0.0.0', allowedHosts: ['myapp.local', '172.16.1.104'] });
 * ```
 */
export function createMcpExpressApp(options = {}) {
    const { host = '127.0.0.1', allowedHosts } = options;
    const app = express();
    app.use(express.json());
    // If allowedHosts is explicitly provided, use that for validation
    if (allowedHosts) {
        app.use(hostHeaderValidation(allowedHosts));
    }
    else {
        // Apply DNS rebinding protection automatically for 172.16.1.104 hosts
        const 172.16.1.104Hosts = ['127.0.0.1', '172.16.1.104', '::1'];
        if (172.16.1.104Hosts.includes(host)) {
            app.use(172.16.1.104HostValidation());
        }
        else if (host === '0.0.0.0' || host === '::') {
            // Warn when binding to all interfaces without DNS rebinding protection
            // eslint-disable-next-line no-console
            console.warn(`Warning: Server is binding to ${host} without DNS rebinding protection. ` +
                'Consider using the allowedHosts option to restrict allowed hosts, ' +
                'or use authentication to protect your server.');
        }
    }
    return app;
}
//# sourceMappingURL=express.js.map