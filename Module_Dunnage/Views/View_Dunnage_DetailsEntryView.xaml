<UserControl
    x:Class="MTM_Receiving_Application.Module_Dunnage.Views.View_Dunnage_DetailsEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MTM_Receiving_Application.Module_Dunnage.Views"
    x:DataType="views:View_Dunnage_DetailsEntryView"
    Loaded="OnLoaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:Converter_IntToVisibility x:Key="IntToVisibilityConverter" />
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,16"
            Spacing="8">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Enter Details" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.GetTip('Dunnage.DetailsEntry'), Mode=OneWay}" />
        </StackPanel>

        <!--  Inventory Notification  -->
        <InfoBar
            Title="Inventory Action Required"
            Grid.Row="1"
            Margin="0,0,0,12"
            IsClosable="False"
            IsOpen="{x:Bind ViewModel.IsInventoryNotificationVisible, Mode=OneWay}"
            Message="{x:Bind ViewModel.InventoryNotificationMessage, Mode=OneWay}"
            Severity="Informational" />

        <!--  Details Form  -->
        <ScrollViewer Grid.Row="2" Style="{StaticResource SharedScrollViewerHorizontalAutoVerticalOffHiddenStyle}">
            <StackPanel Spacing="12">
                <!--  PO Number and Location Card  -->
                <Border
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  PO Number  -->
                        <StackPanel Grid.Column="0" Spacing="6">
                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="PO Number" />
                            <TextBox
                                x:Name="PoNumberTextBox"
                                HorizontalAlignment="Stretch"
                                PlaceholderText="{x:Bind ViewModel.GetPlaceholder('Field.PONumber'), Mode=OneWay}"
                                Text="{x:Bind ViewModel.PoNumber, Mode=TwoWay}" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Leave blank for non-PO dunnage" />
                        </StackPanel>

                        <!--  Location  -->
                        <StackPanel Grid.Column="1" Spacing="6">
                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Location" />
                            <TextBox
                                x:Name="LocationTextBox"
                                HorizontalAlignment="Stretch"
                                LostFocus="LocationTextBox_LostFocus"
                                PlaceholderText="{x:Bind ViewModel.GetPlaceholder('Field.Location'), Mode=OneWay}"
                                Text="{x:Bind ViewModel.Location, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Where this dunnage is located" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!--  Specifications Card  -->
                <Border
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Load Details" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="Review the saved part details below and enter anything needed for this load."
                            TextWrapping="Wrap" />

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Column 1: Text Specs  -->
                            <StackPanel
                                Grid.Column="0"
                                Spacing="4"
                                Visibility="{x:Bind ViewModel.HasTextSpecs, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.TextSpecs, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,0,0,6" Spacing="2">
                                                <TextBlock
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding SpecName}" />
                                                <TextBox Width="180" Text="{Binding Value, Mode=TwoWay}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>

                            <!--  Column 2: Number Specs  -->
                            <StackPanel
                                Grid.Column="1"
                                Spacing="4"
                                Visibility="{x:Bind ViewModel.HasNumberSpecs, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.NumberSpecs, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,0,0,6" Spacing="2">
                                                <TextBlock
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding SpecName}" />
                                                <TextBox Width="180" Text="{Binding Value, Mode=TwoWay}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>

                            <!--  Column 3: Boolean Specs  -->
                            <StackPanel
                                Grid.Column="2"
                                Spacing="4"
                                Visibility="{x:Bind ViewModel.HasBooleanSpecs, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.BooleanSpecs, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox
                                                Margin="0,0,0,6"
                                                Content="{Binding SpecName}"
                                                IsChecked="{Binding Value, Mode=TwoWay}" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>

                            <!--  Column 4: Choice Specs  -->
                            <StackPanel
                                Grid.Column="3"
                                Spacing="4"
                                Visibility="{x:Bind ViewModel.HasChoiceSpecs, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.ChoiceSpecs, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,0,0,6" Spacing="2">
                                                <TextBlock
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding SpecName}" />
                                                <ComboBox
                                                    Width="180"
                                                    ItemsSource="{Binding Choices}"
                                                    SelectedItem="{Binding Value, Mode=TwoWay}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </Grid>

                        <!--  Placeholder when no specs  -->
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="There are no extra details to enter for this part."
                            Visibility="{x:Bind ViewModel.SpecInputs.Count, Mode=OneWay, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter='Inverse'}" />
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
