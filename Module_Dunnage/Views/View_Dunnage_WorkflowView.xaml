<Page
    x:Class="MTM_Receiving_Application.Module_Dunnage.Views.View_Dunnage_WorkflowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MTM_Receiving_Application.Module_Dunnage.ViewModels"
    xmlns:views="using:MTM_Receiving_Application.Module_Dunnage.Views"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Content Area  -->
        <Grid Grid.Row="1" Margin="16,12,16,16">
            <!--  Mode Selection View  -->
            <views:View_Dunnage_ModeSelectionView Visibility="{x:Bind ViewModel.IsModeSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_Dialog_ImagePartSearch Visibility="{x:Bind ViewModel.IsImagePartSearchVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <!--  Wizard Steps  -->
            <views:View_dunnage_typeselectionView Visibility="{x:Bind ViewModel.IsTypeSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_PartSelectionView x:Name="PartSelectionView" Visibility="{x:Bind ViewModel.IsPartSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_QuantityEntryView x:Name="QuantityEntryView" Visibility="{x:Bind ViewModel.IsQuantityEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_DetailsEntryView x:Name="DetailsEntryView" Visibility="{x:Bind ViewModel.IsDetailsEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_ReviewView x:Name="ReviewView" Visibility="{x:Bind ViewModel.IsReviewVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <!--  Bulk Operation Views  -->
            <views:View_Dunnage_ManualEntryView x:Name="ManualEntryView" Visibility="{x:Bind ViewModel.IsManualEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <views:View_Dunnage_EditModeView x:Name="EditModeView" Visibility="{x:Bind ViewModel.IsEditModeVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>

        <!--  Navigation Buttons  -->
        <Grid
            Grid.Row="2"
            Margin="24"
            IsHitTestVisible="{x:Bind ViewModel.CanNavigate, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Left-aligned buttons  -->
            <StackPanel
                Grid.Column="0"
                HorizontalAlignment="Left"
                Orientation="Horizontal"
                Spacing="12">
                <!--  Mode Selection Button (visible in wizard steps)  -->
                <Button
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Command="{x:Bind ViewModel.ClearLabelDataCommand}"
                    Content="Clear All Labels"
                    Foreground="Red"
                    IsEnabled="{x:Bind ViewModel.CanClearLabelData, Mode=OneWay}"
                    ToolTipService.ToolTip="Archive the active dunnage label queue to history and clear the queue"
                    Visibility="{x:Bind ViewModel.IsModeSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button
                    Command="{x:Bind ViewModel.ReturnToModeSelectionCommand}"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsImagePartSearchVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Command="{x:Bind ViewModel.ReturnToModeSelectionCommand}"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsTypeSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Command="{x:Bind ViewModel.ReturnToModeSelectionCommand}"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsPartSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Command="{x:Bind ViewModel.ReturnToModeSelectionCommand}"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsQuantityEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Command="{x:Bind ViewModel.ReturnToModeSelectionCommand}"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsDetailsEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button
                    Click="OnModeSelectionClick"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsManualEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Click="OnModeSelectionClick"
                    Content="Mode Selection"
                    ToolTipService.ToolTip="Return to mode selection (clears current work)"
                    Visibility="{x:Bind ViewModel.IsEditModeVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button
                    Command="{x:Bind ViewModel.ClearLabelDataCommand}"
                    Content="Clear All Labels"
                    Foreground="Red"
                    IsEnabled="{x:Bind ViewModel.CanClearLabelData, Mode=OneWay}"
                    ToolTipService.ToolTip="Archive the active dunnage label queue to history and clear the queue"
                    Visibility="{x:Bind ViewModel.IsModeSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='Inverse'}" />
            </StackPanel>

            <!--  Right-aligned utility buttons  -->
            <StackPanel
                Grid.Column="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    Command="{x:Bind ViewModel.OpenDunnageLabelCommand}"
                    Style="{StaticResource WindowChromeStyle}"
                    ToolTipService.ToolTip="Open Dunnage Label">
                    <FontIcon FontSize="16" Glyph="&#xE8A5;" />
                </Button>
                <Button
                    Click="HelpButton_Click"
                    Style="{StaticResource WindowChromeStyle}"
                    ToolTipService.ToolTip="Click for help about the current step">
                    <FontIcon FontSize="16" Glyph="&#xE946;" />
                </Button>
            </StackPanel>

            <!--  Right-aligned navigation buttons (wizard flow)  -->
            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12">
                <!--  Back Button (not visible on first wizard step)  -->
                <Button
                    Click="OnBackClick"
                    ToolTipService.ToolTip="Go back to previous step"
                    Visibility="{x:Bind ViewModel.IsPartSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE760;" />
                        <TextBlock Text="Back" />
                    </StackPanel>
                </Button>
                <Button
                    Click="OnBackClick"
                    ToolTipService.ToolTip="Go back to previous step"
                    Visibility="{x:Bind ViewModel.IsQuantityEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE760;" />
                        <TextBlock Text="Back" />
                    </StackPanel>
                </Button>
                <Button
                    Click="OnBackClick"
                    ToolTipService.ToolTip="Go back to previous step"
                    Visibility="{x:Bind ViewModel.IsDetailsEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE760;" />
                        <TextBlock Text="Back" />
                    </StackPanel>
                </Button>
                <Button
                    Click="OnBackClick"
                    ToolTipService.ToolTip="Go back to previous step"
                    Visibility="{x:Bind ViewModel.IsReviewVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE760;" />
                        <TextBlock Text="Back" />
                    </StackPanel>
                </Button>

                <!--  Next Button (wizard flow)  -->
                <Button
                    Click="OnNextClick"
                    IsEnabled="{x:Bind PartSelectionView.ViewModel.IsPartSelected, Mode=OneWay}"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Proceed to next step"
                    Visibility="{x:Bind ViewModel.IsPartSelectionVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="Next" />
                        <FontIcon Glyph="&#xE761;" />
                    </StackPanel>
                </Button>
                <Button
                    Click="OnNextClick"
                    IsEnabled="{x:Bind QuantityEntryView.ViewModel.IsValid, Mode=OneWay}"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Proceed to next step"
                    Visibility="{x:Bind ViewModel.IsQuantityEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="Next" />
                        <FontIcon Glyph="&#xE761;" />
                    </StackPanel>
                </Button>
                <Button
                    Click="OnSaveAndReviewClick"
                    IsEnabled="{x:Bind DetailsEntryView.ViewModel.CanProceedToNextStep, Mode=OneWay}"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Save entry and proceed to review"
                    Visibility="{x:Bind ViewModel.IsDetailsEntryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="Review" />
                        <FontIcon Glyph="&#xF28B;" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
