<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="MTM_Receiving_Application.Module_Dunnage.Views.View_Dunnage_EditModeColumnChooserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Choose visible columns"
    Opened="Dialog_Opened">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1600</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">900</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">760</x:Double>
    </ContentDialog.Resources>

    <Grid
        x:Name="RootGrid"
        MinWidth="900"
        RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <TextBlock
                Margin="0,0,260,0"
                VerticalAlignment="Center"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="Choose which columns appear in Dunnage Edit Mode. Fields are grouped so the list is easier to scan and updated automatically from the live grid columns."
                TextWrapping="Wrap" />

            <Border
                Padding="12"
                HorizontalAlignment="Right"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <StackPanel Spacing="4">
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Always Shown" />
                    <TextBlock
                        x:Name="AlwaysVisibleTextBlock"
                        FontWeight="SemiBold"
                        TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>
        </Grid>

        <Border
            Grid.Row="1"
            Padding="16"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="6">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Click="SelectAllButton_Click"
                    Content="Select All" />
                <Button
                    Grid.Column="1"
                    Click="ClearAllButton_Click"
                    Content="Clear All" />
                <Button
                    Grid.Column="2"
                    Click="ResetButton_Click"
                    Content="Reset" />

                <TextBlock
                    Grid.Column="3"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="Apply saves the current visibility choices for this edit session. Cancel leaves the grid exactly as it is now."
                    TextWrapping="Wrap" />
            </Grid>
        </Border>

        <Border
            Grid.Row="2"
            Padding="16"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="6">
            <ScrollViewer
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled">
                <StackPanel x:Name="GroupsPanel" Spacing="16" />
            </ScrollViewer>
        </Border>

        <Border
            Grid.Row="3"
            Margin="0,12,0,0"
            Padding="0,12,0,0"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <StackPanel
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="FooterCancelButton"
                    MinWidth="100"
                    Click="OnFooterCancelButtonClick"
                    Content="Cancel" />
                <Button
                    x:Name="FooterApplyButton"
                    MinWidth="100"
                    Click="OnFooterApplyButtonClick"
                    Content="Apply"
                    Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</ContentDialog>
