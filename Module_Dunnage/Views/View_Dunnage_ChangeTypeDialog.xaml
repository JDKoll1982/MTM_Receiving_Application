<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="MTM_Receiving_Application.Module_Dunnage.Views.View_Dunnage_ChangeTypeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Change Dunnage Type"
    PrimaryButtonText="Change Type"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    PrimaryButtonClick="OnPrimaryButtonClick">

    <StackPanel
        MinWidth="440"
        MaxWidth="580"
        Spacing="12">
        <TextBlock
            x:Name="PartSummaryTextBlock"
            Style="{StaticResource BodyTextBlockStyle}"
            TextWrapping="Wrap" />

        <ComboBox
            x:Name="TypesComboBox"
            Header="New Dunnage Type"
            HorizontalAlignment="Stretch"
            DisplayMemberPath="TypeName"
            PlaceholderText="Select a dunnage type"
            SelectionChanged="OnTypesSelectionChanged" />

        <TextBlock
            x:Name="ReconcileTextBlock"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            TextWrapping="Wrap" />

        <StackPanel x:Name="RequiredFieldsPanel" />

        <TextBlock
            x:Name="ErrorTextBlock"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            TextWrapping="Wrap"
            Visibility="Collapsed" />
    </StackPanel>
</ContentDialog>
