/* WinUI3-Inspired Styles for MTM Waitlist Application Mockups */
/* Mimics WinUI3 controls using HTML/CSS */

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: var(--font-family);
  font-size: var(--font-size-body);
  color: var(--text-primary);
  background-color: var(--background-primary);
  line-height: 1.5;
  overflow: hidden;
}

/* =========================
   App Shell Layout
   WinUI3 Equivalent: NavigationView + Frame
   ========================= */

.app-shell {
  display: grid;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto 1fr auto;
  height: 100vh;
  width: 100vw;
}

/* =========================
   Navigation Pane (Left Sidebar)
   WinUI3 Equivalent: NavigationView
   ========================= */

.nav-pane {
  grid-column: 1;
  grid-row: 1 / -1;
  width: 320px;
  background-color: var(--nav-pane-background);
  border-right: 1px solid var(--divider-stroke);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

.nav-pane-header {
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--divider-stroke);
}

.nav-pane-header h1 {
  font-size: var(--font-size-title);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
}

.nav-pane-header .subtitle {
  font-size: var(--font-size-caption);
  color: var(--text-secondary);
  margin-top: var(--spacing-xs);
}

/* Navigation Menu Items */
.nav-menu {
  list-style: none;
  padding: var(--spacing-sm) 0;
  flex: 1;
}

.nav-item {
  margin: 0;
}

.nav-item-link {
  display: flex;
  align-items: center;
  padding: var(--spacing-md) var(--spacing-lg);
  color: var(--text-primary);
  text-decoration: none;
  border-left: 4px solid transparent;
  transition: background-color 0.1s ease, border-color 0.1s ease;
}

.nav-item-link:hover {
  background-color: var(--nav-item-background-hover);
}

.nav-item-link:active {
  background-color: var(--nav-item-background-pressed);
}

.nav-item-link.selected {
  background-color: var(--nav-item-background-selected);
  border-left-color: var(--nav-item-border-selected);
}

.nav-item-icon {
  font-size: 20px;
  margin-right: var(--spacing-md);
  width: 24px;
  text-align: center;
}

.nav-item-text {
  font-size: var(--font-size-body);
  font-weight: var(--font-weight-regular);
}

.nav-separator {
  height: 1px;
  background-color: var(--divider-stroke);
  margin: var(--spacing-sm) var(--spacing-lg);
}

/* Navigation Footer */
.nav-footer {
  border-top: 1px solid var(--divider-stroke);
  padding: var(--spacing-sm) 0;
}

/* =========================
   Content Area
   WinUI3 Equivalent: Frame / Page
   ========================= */

.content-area {
  grid-column: 2;
  grid-row: 2;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* Command Bar (Top Actions) */
/* WinUI3 Equivalent: CommandBar */
.command-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-md) var(--spacing-lg);
  background-color: var(--commandbar-background);
  border-bottom: 1px solid: var(--commandbar-border);
  min-height: 48px;
}

.command-bar-left,
.command-bar-right {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.page-title {
  font-size: var(--font-size-subtitle);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
}

/* Search Box */
/* WinUI3 Equivalent: AutoSuggestBox */
.search-box {
  position: relative;
  width: 300px;
}

.search-box input {
  width: 100%;
  padding: var(--spacing-sm) var(--spacing-md);
  padding-left: 36px;
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-small);
  background-color: var(--card-background);
  color: var(--text-primary);
  font-family: var(--font-family);
  font-size: var(--font-size-body);
}

.search-box input:focus {
  outline: 2px solid var(--accent-default);
  outline-offset: -2px;
}

.search-box::before {
  content: "\E721"; /* Search icon */
  font-family: "Segoe Fluent Icons", "Segoe MDL2 Assets";
  position: absolute;
  left: var(--spacing-md);
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-secondary);
  pointer-events: none;
}

/* Main Content Container */
.main-content {
  flex: 1;
  overflow-y: auto;
  padding: var(--spacing-lg);
}

/* =========================
   Cards & Panels
   WinUI3 Equivalent: Card / Panel
   ========================= */

.card {
  background-color: var(--card-background);
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-medium);
  box-shadow: var(--shadow-card);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-md);
}

.card-title {
  font-size: var(--font-size-subtitle);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
}

.card-content {
  color: var(--text-secondary);
}

/* =========================
   Tables (DataGrid/ListView)
   WinUI3 Equivalent: ListView with GridView
   ========================= */

.winui-table {
  width: 100%;
  border-collapse: collapse;
  background-color: var(--card-background);
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-medium);
  overflow: hidden;
}

.winui-table thead {
  background-color: var(--background-tertiary);
  border-bottom: 1px solid var(--divider-stroke);
}

.winui-table th {
  text-align: left;
  padding: var(--spacing-md);
  font-size: var(--font-size-caption);
  font-weight: var(--font-weight-semibold);
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.winui-table tbody tr {
  border-bottom: 1px solid var(--divider-stroke);
  transition: background-color 0.1s ease;
}

.winui-table tbody tr:last-child {
  border-bottom: none;
}

.winui-table tbody tr:hover {
  background-color: var(--card-background-hover);
}

.winui-table tbody tr.selected {
  background-color: var(--nav-item-background-selected);
}

.winui-table td {
  padding: var(--spacing-md);
  color: var(--text-primary);
  vertical-align: middle;
}

/* Priority/Status Indicators */
.priority-badge,
.status-badge {
  display: inline-block;
  padding: 2px 8px;
  border-radius: var(--corner-radius-small);
  font-size: var(--font-size-caption);
  font-weight: var(--font-weight-semibold);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.priority-high,
.wait-critical {
  background-color: rgba(232, 17, 35, 0.1);
  color: var(--error-color);
}

.priority-medium,
.wait-warning {
  background-color: rgba(255, 200, 61, 0.1);
  color: var(--warning-color);
}

.priority-low,
.wait-normal {
  background-color: rgba(16, 124, 16, 0.1);
  color: var(--success-color);
}

/* Task Type Badges (Material Handler) */
.task-badge {
  display: inline-block;
  padding: 4px 8px;
  border-radius: var(--corner-radius-small);
  font-size: var(--font-size-caption);
  font-weight: var(--font-weight-semibold);
  color: #FFFFFF;
}

.task-receiving { background-color: var(--task-receiving); }
.task-dunnage { background-color: var(--task-dunnage); }
.task-routing { background-color: var(--task-routing); }
.task-press { background-color: var(--task-press); }

/* =========================
   Buttons
   WinUI3 Equivalent: Button / AppBarButton
   ========================= */

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm) var(--spacing-lg);
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-small);
  background-color: var(--card-background);
  color: var(--text-primary);
  font-family: var(--font-family);
  font-size: var(--font-size-body);
  font-weight: var(--font-weight-regular);
  cursor: pointer;
  transition: all 0.1s ease;
  text-decoration: none;
}

.btn:hover:not(:disabled) {
  background-color: var(--card-background-hover);
}

.btn:active:not(:disabled) {
  background-color: var(--card-background-pressed);
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn-accent {
  background-color: var(--accent-default);
  color: var(--text-on-accent);
  border-color: var(--accent-default);
}

.btn-accent:hover:not(:disabled) {
  background-color: var(--accent-secondary);
  border-color: var(--accent-secondary);
}

.btn-accent:active:not(:disabled) {
  background-color: var(--accent-tertiary);
  border-color: var(--accent-tertiary);
}

/* Icon Buttons (AppBarButton style) */
.btn-icon {
  width: 36px;
  height: 36px;
  padding: 0;
  border: none;
  background-color: transparent;
  color: var(--text-primary);
  font-size: 16px;
  cursor: pointer;
  border-radius: var(--corner-radius-small);
  transition: background-color 0.1s ease;
}

.btn-icon:hover:not(:disabled) {
  background-color: var(--nav-item-background-hover);
}

.btn-icon:active:not(:disabled) {
  background-color: var(--nav-item-background-pressed);
}

.btn-icon:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

/* Action Buttons Row */
.action-buttons {
  display: flex;
  gap: var(--spacing-xs);
}

/* Specific Action Button Colors */
.btn-call { color: var(--info-color); }
.btn-message { color: var(--success-color); }
.btn-alert { color: var(--warning-color); }
.btn-edit { color: var(--text-secondary); }
.btn-cancel { color: var(--error-color); }
.btn-complete { color: var(--success-color); }

/* =========================
   Floating Action Button (FAB)
   ========================= */

.fab {
  position: fixed;
  bottom: 24px;
  right: 24px;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background-color: var(--fab-background);
  color: #FFFFFF;
  border: none;
  box-shadow: var(--fab-shadow);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24px;
  transition: all 0.2s ease;
  z-index: 1000;
}

.fab:hover {
  background-color: var(--fab-background-hover);
  transform: scale(1.05);
}

.fab:active {
  transform: scale(0.95);
}

.fab.hidden {
  display: none;
}

/* =========================
   Modals / Content Dialogs
   WinUI3 Equivalent: ContentDialog
   ========================= */

.dialog-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2000;
}

.dialog-overlay.hidden {
  display: none;
}

.dialog {
  background-color: var(--card-background);
  border-radius: var(--corner-radius-medium);
  box-shadow: var(--shadow-dialog);
  min-width: 400px;
  max-width: 600px;
  max-height: 80vh;
  display: flex;
  flex-direction: column;
}

.dialog-header {
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--divider-stroke);
}

.dialog-title {
  font-size: var(--font-size-subtitle);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
}

.dialog-content {
  padding: var(--spacing-lg);
  overflow-y: auto;
  flex: 1;
}

.dialog-footer {
  padding: var(--spacing-lg);
  border-top: 1px solid var(--divider-stroke);
  display: flex;
  justify-content: flex-end;
  gap: var(--spacing-sm);
}

/* =========================
   Form Controls
   ========================= */

.form-group {
  margin-bottom: var(--spacing-lg);
}

.form-label {
  display: block;
  font-size: var(--font-size-body);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: var(--spacing-xs);
}

.form-input,
.form-select,
.form-textarea {
  width: 100%;
  padding: var(--spacing-sm) var(--spacing-md);
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-small);
  background-color: var(--card-background);
  color: var(--text-primary);
  font-family: var(--font-family);
  font-size: var(--font-size-body);
}

.form-input:focus,
.form-select:focus,
.form-textarea:focus {
  outline: 2px solid var(--accent-default);
  outline-offset: -2px;
}

.form-textarea {
  resize: vertical;
  min-height: 80px;
}

/* Toggle Switch */
/* WinUI3 Equivalent: ToggleSwitch */
.toggle-switch {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-md);
  cursor: pointer;
  user-select: none;
}

.toggle-track {
  position: relative;
  width: 40px;
  height: 20px;
  background-color: var(--border-color);
  border-radius: 10px;
  transition: background-color 0.2s ease;
}

.toggle-switch input {
  position: absolute;
  opacity: 0;
}

.toggle-switch input:checked ~ .toggle-track {
  background-color: var(--accent-default);
}

.toggle-thumb {
  position: absolute;
  top: 2px;
  left: 2px;
  width: 16px;
  height: 16px;
  background-color: #FFFFFF;
  border-radius: 50%;
  transition: transform 0.2s ease;
}

.toggle-switch input:checked ~ .toggle-track .toggle-thumb {
  transform: translateX(20px);
}

/* =========================
   Charts Container
   ========================= */

.chart-container {
  background-color: var(--card-background);
  border: 1px solid var(--border-color);
  border-radius: var(--corner-radius-medium);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
}

.chart-container h3 {
  font-size: var(--font-size-subtitle);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: var(--spacing-md);
}

.chart-svg {
  width: 100%;
  height: auto;
  font-family: var(--font-family);
  fill: var(--text-secondary);
}

/* =========================
   Info Bar (Notifications)
   WinUI3 Equivalent: InfoBar
   ========================= */

.info-bar {
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  border-radius: var(--corner-radius-small);
  margin-bottom: var(--spacing-md);
  border-left: 4px solid;
}

.info-bar-info {
  background-color: rgba(0, 120, 212, 0.1);
  border-color: var(--info-color);
}

.info-bar-success {
  background-color: rgba(16, 124, 16, 0.1);
  border-color: var(--success-color);
}

.info-bar-warning {
  background-color: rgba(255, 200, 61, 0.1);
  border-color: var(--warning-color);
}

.info-bar-error {
  background-color: rgba(232, 17, 35, 0.1);
  border-color: var(--error-color);
}

.info-bar-icon {
  font-size: 20px;
  flex-shrink: 0;
}

.info-bar-content {
  flex: 1;
}

.info-bar-title {
  font-weight: var(--font-weight-semibold);
  margin-bottom: var(--spacing-xs);
}

.info-bar-message {
  font-size: var(--font-size-caption);
  color: var(--text-secondary);
}

/* =========================
   Status Bar (Bottom)
   ========================= */

.status-bar {
  grid-column: 2;
  grid-row: 3;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-sm) var(--spacing-lg);
  background-color: var(--commandbar-background);
  border-top: 1px solid var(--commandbar-border);
  min-height: 32px;
  font-size: var(--font-size-caption);
  color: var(--text-secondary);
}

.status-indicators {
  display: flex;
  gap: var(--spacing-lg);
}

.status-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
}

/* =========================
   Utility Classes
   ========================= */

.hidden {
  display: none !important;
}

.text-center {
  text-align: center;
}

.text-right {
  text-align: right;
}

.mt-0 { margin-top: 0; }
.mt-sm { margin-top: var(--spacing-sm); }
.mt-md { margin-top: var(--spacing-md); }
.mt-lg { margin-top: var(--spacing-lg); }

.mb-0 { margin-bottom: 0; }
.mb-sm { margin-bottom: var(--spacing-sm); }
.mb-md { margin-bottom: var(--spacing-md); }
.mb-lg { margin-bottom: var(--spacing-lg); }

.p-0 { padding: 0; }
.p-sm { padding: var(--spacing-sm); }
.p-md { padding: var(--spacing-md); }
.p-lg { padding: var(--spacing-lg); }

.flex {
  display: flex;
}

.flex-column {
  flex-direction: column;
}

.gap-sm {
  gap: var(--spacing-sm);
}

.gap-md {
  gap: var(--spacing-md);
}

.gap-lg {
  gap: var(--spacing-lg);
}

.items-center {
  align-items: center;
}

.justify-between {
  justify-content: space-between;
}

.justify-center {
  justify-content: center;
}
