<Page
    x:Class="MTM_Receiving_Application.Module_ShipRec_Tools.Views.View_Tool_CustomerPullPackPullList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MTM_Receiving_Application.Module_ShipRec_Tools.Models">
    <Grid Padding="18">
        <ScrollViewer Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}">
            <StackPanel Spacing="12">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind PrintContext.Title, Mode=OneTime}" />
                <TextBlock
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{x:Bind PrintContext.ActiveFiltersSummary, Mode=OneTime}"
                    TextWrapping="WrapWholeWords" />
                <ItemsControl ItemsSource="{x:Bind PrintContext.GroupedSubPartTotals, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_CustomerPullPack_SubPartPrintGroup">
                            <Border
                                Margin="0,0,0,8"
                                Padding="10"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                                <StackPanel Spacing="8">
                                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind SubPartId, Mode=OneWay}" />
                                    <TextBlock Text="{x:Bind TotalQuantityNeeded, Mode=OneWay}" />
                                    <TextBlock Text="{x:Bind TotalQuantityOnHand, Mode=OneWay}" />
                                    <ItemsControl ItemsSource="{x:Bind ChosenLocations, Mode=OneWay}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <TextBlock Text="{x:Bind Mode=OneWay}" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
