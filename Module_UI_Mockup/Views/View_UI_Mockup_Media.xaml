<Page
    x:Class="Module_UI_Mockup.Views.View_UI_Mockup_Media"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <ScrollViewer>
        <StackPanel Padding="40" Spacing="24">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Media &amp; Visual Elements"/>

            <!-- Image -->
            <StackPanel Spacing="16">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Image"/>
                <Image Source="/Assets/StoreLogo.png" Width="100" Height="100" Stretch="Uniform"/>
            </StackPanel>

            <!-- PersonPicture -->
            <StackPanel Spacing="16">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="PersonPicture"/>
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <PersonPicture DisplayName="John Doe" Initials="JD"/>
                    <PersonPicture DisplayName="Jane Smith" Initials="JS"/>
                </StackPanel>
            </StackPanel>

            <!-- AnimatedIcon (using CommunityToolkit animations) -->
            <StackPanel Spacing="16">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Animated Icons"/>
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <!-- Scale animation on hover -->
                    <Button x:Name="AnimatedButton1" Width="120" Height="120" CornerRadius="8">
                        <Button.Resources>
                            <Storyboard x:Name="ScaleUpStoryboard">
                                <DoubleAnimation Storyboard.TargetName="AnimatedButton1Icon" 
                                               Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1.3" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetName="AnimatedButton1Icon" 
                                               Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1.3" Duration="0:0:0.2"/>
                            </Storyboard>
                            <Storyboard x:Name="ScaleDownStoryboard">
                                <DoubleAnimation Storyboard.TargetName="AnimatedButton1Icon" 
                                               Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1.0" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetName="AnimatedButton1Icon" 
                                               Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1.0" Duration="0:0:0.2"/>
                            </Storyboard>
                        </Button.Resources>
                        <FontIcon x:Name="AnimatedButton1Icon" 
                                  Glyph="&#xE721;" 
                                  FontSize="32"
                                  RenderTransformOrigin="0.5,0.5">
                            <FontIcon.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </Button>
                    
                    <!-- Rotation animation on click -->
                    <ToggleButton x:Name="RotateButton" Width="120" Height="120" CornerRadius="8" Checked="RotateButton_Checked" Unchecked="RotateButton_Unchecked">
                        <FontIcon x:Name="RotateIcon" 
                                  Glyph="&#xE72C;" 
                                  FontSize="32"
                                  RenderTransformOrigin="0.5,0.5">
                            <FontIcon.RenderTransform>
                                <RotateTransform x:Name="RotateTransform" Angle="0"/>
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </ToggleButton>
                    
                    <!-- Pulse animation (continuous) -->
                    <Border Width="120" Height="120" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <FontIcon Glyph="&#xE8B7;" FontSize="32" Foreground="{ThemeResource AccentFillColorDefaultBrush}">
                            <FontIcon.RenderTransform>
                                <ScaleTransform x:Name="PulseScale" ScaleX="1" ScaleY="1"/>
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </Border>
                </StackPanel>
                <TextBlock Text="Hover, click toggle, and watch the pulse animation" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- WinUI 3 AnimatedIcon with FontIcon -->
            <StackPanel Spacing="16">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="AnimatedIcon (Custom Transitions)"/>
                <TextBlock Text="AnimatedIcon requires custom AnimatedVisualSource - using animated FontIcons instead" 
                           Style="{StaticResource CaptionTextBlockStyle}" 
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <!-- Scale + Rotate on Click -->
                    <Button Width="100" Height="100" Click="IconButton1_Click">
                        <FontIcon x:Name="AnimIcon1" Glyph="&#xE734;" FontSize="32">
                            <FontIcon.RenderTransform>
                                <CompositeTransform x:Name="AnimTransform1" ScaleX="1" ScaleY="1" Rotation="0"/>
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </Button>

                    <!-- Fade Toggle -->
                    <ToggleButton Width="100" Height="100" Checked="IconToggle2_Checked" Unchecked="IconToggle2_Unchecked">
                        <FontIcon x:Name="AnimIcon2" Glyph="&#xE713;" FontSize="32" Opacity="1"/>
                    </ToggleButton>

                    <!-- Color Change Toggle -->
                    <ToggleButton Width="100" Height="100" Checked="IconToggle3_Checked" Unchecked="IconToggle3_Unchecked">
                        <FontIcon x:Name="AnimIcon3" Glyph="&#xE8FB;" FontSize="32"/>
                    </ToggleButton>
                </StackPanel>
                <TextBlock Text="Click and toggle to see transitions (scale, rotate, fade, color)" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- Shapes -->
            <StackPanel Spacing="16">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Shapes"/>
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <Rectangle Width="50" Height="50" Fill="{ThemeResource AccentFillColorDefaultBrush}" RadiusX="4" RadiusY="4"/>
                    <Ellipse Width="50" Height="50" Fill="{ThemeResource SystemFillColorSuccessBrush}"/>
                    <Polygon Points="25,0 50,50 0,50" Fill="{ThemeResource SystemFillColorAttentionBrush}"/>
                </StackPanel>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
