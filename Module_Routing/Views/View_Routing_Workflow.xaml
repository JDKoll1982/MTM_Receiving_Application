<Page
    x:Class="MTM_Receiving_Application.Module_Routing.Views.View_Routing_Workflow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MTM_Receiving_Application.Module_Routing.ViewModels"
    xmlns:views="using:MTM_Receiving_Application.Module_Routing.Views"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Tabs -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Edit" Label="Label Entry" Command="{x:Bind ViewModel.NavigateToLabelEntryCommand}"/>
            <AppBarButton Icon="Calendar" Label="History" Command="{x:Bind ViewModel.NavigateToHistoryCommand}"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Print" Label="Print Labels" Command="{x:Bind ViewModel.PrintLabelsCommand}"/>
        </CommandBar>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <!-- Label Entry View -->
            <views:View_Routing_LabelEntry x:Name="LabelEntryView"
                                            Visibility="{x:Bind IsLabelEntryStep(ViewModel.CurrentStep), Mode=OneWay}"/>

            <!-- History View -->
            <views:View_Routing_History x:Name="HistoryView"
                                         Visibility="{x:Bind IsHistoryStep(ViewModel.CurrentStep), Mode=OneWay}"/>
        </Grid>
    </Grid>
</Page>
