<UserControl x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_HeatLot" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls" 
             xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:models="using:MTM_Receiving_Application.Module_Receiving.Models" 
             x:Name="Root" 
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:Converter_StringFormat x:Key="StringFormatConverter" />
        <converters:Converter_IntToVisibility x:Key="IntToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Padding="20">
        <!-- Replaced layout-breaking StackPanel with a functional Grid -->
        <Grid RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Header Card Section -->
                <RowDefinition Height="*" />
                <!-- ScrollViewer consumes remaining space -->
            </Grid.RowDefinitions>

            <!-- Header Card Section -->
            <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="2">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="16" Glyph="&#xE7B8;" />
                            <TextBlock VerticalAlignment="Center" FontSize="16" Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.HeatLotHeaderText, Mode=OneWay}" />
                        </StackPanel>
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" Text="Enter heat and lot details for each load." TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="8">
                        <Button Padding="16,8" HorizontalAlignment="Stretch" Command="{x:Bind ViewModel.AutoFillCommand}" Content="{x:Bind ViewModel.HeatLotAutoFillText, Mode=OneWay}" CornerRadius="8" FontSize="16" Style="{StaticResource AccentButtonStyle}" ToolTipService.ToolTip="{x:Bind ViewModel.HeatLotAutoFillTooltipText, Mode=OneWay}" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox Width="220" Header="Preset Heat/Lot Fillers" ItemsSource="{x:Bind ViewModel.HeatLotPresetFillers, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedHeatLotPresetFiller, Mode=TwoWay}" />
                            <Button Padding="12,8" VerticalAlignment="Center" Command="{x:Bind ViewModel.ApplyHeatLotPresetCommand}" ToolTipService.ToolTip="Overwrite all heat/lot values with the selected preset">
                                <FontIcon FontSize="16" Glyph="&#xE790;" />
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Fully Dynamic ScrollViewer Layout Panel -->
            <ScrollViewer Grid.Row="1" 
                          VerticalAlignment="Stretch"
                          HorizontalScrollBarVisibility="Disabled" 
                          HorizontalScrollMode="Disabled" 
                          VerticalScrollBarVisibility="Auto" 
                          VerticalScrollMode="Enabled" 
                          Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}">

                <ItemsControl x:Name="LoadsItemsControl" FlowDirection="LeftToRight" ItemsSource="{Binding ElementName=Root, Path=ViewModel.Loads}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!-- FIXED: Added Orientation="Horizontal" to ensure rows hug content height -->
                            <controls:UniformGrid Columns="3" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Stretch" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_ReceivingLoad">
                            <Grid MinHeight="90" Margin="0,0,4,4" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Glyph="&#xE7B8;" />
                                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding LoadNumber, Converter={StaticResource StringFormatConverter}, ConverterParameter='Load #{0}'}" />
                                    </StackPanel>
                                </StackPanel>

                                <Grid Grid.Column="1" VerticalAlignment="Center" RowSpacing="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBox Grid.Row="0"
                                              AutomationProperties.Name="{Binding ElementName=Root, Path=ViewModel.HeatLotAccessibilityName}" Header="Heat or Lot Number" HorizontalAlignment="Stretch" PlaceholderText="{Binding ElementName=Root, Path=ViewModel.HeatLotFieldPlaceholderText}" Text="{x:Bind HeatLotNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Row="1" AutomationProperties.Name="{x:Bind UserSetVariableAccessibilityName, Mode=OneWay}" Header="{x:Bind UserSetVariableFieldHeaderText, Mode=OneWay}" HorizontalAlignment="Stretch" PlaceholderText="{x:Bind UserSetVariableFieldPlaceholderText, Mode=OneWay}" Text="{x:Bind UserSetVariable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="{x:Bind UserSetVariableFieldVisibility, Mode=OneWay}" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
