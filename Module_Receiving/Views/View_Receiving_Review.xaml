<UserControl
    x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_Review"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Padding="24" RowSpacing="16" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Content Section with View Toggle -->
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <!-- Single Entry View -->
            <Grid Visibility="{x:Bind ViewModel.IsSingleView, Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Entry Counter -->
                <Border Grid.Row="0" 
                       Background="{ThemeResource AccentFillColorDefaultBrush}"
                       Padding="12,6"
                       HorizontalAlignment="Stretch"
                       CornerRadius="8,8,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8FD;" 
                                 FontSize="16"
                                 Foreground="White"/>
                        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}"
                                  Foreground="White">
                               <Run Text="{x:Bind ViewModel.ReviewEntryLabelText, Mode=OneWay}"/>
                            <Run Text="{x:Bind ViewModel.DisplayIndex, Mode=OneWay}" 
                                 FontWeight="Bold"/>
                               <Run Text="{x:Bind ViewModel.ReviewOfLabelText, Mode=OneWay}"/>
                            <Run Text="{x:Bind ViewModel.Loads.Count, Mode=OneWay}" 
                                 FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Single Entry Form -->
                <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                           BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                           BorderThickness="1,0,1,1"
                           CornerRadius="0,0,8,8"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Padding="20">
                        <controls:UniformGrid Columns="3" ColumnSpacing="8" RowSpacing="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">


                            <!-- PO Number -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                       Background="{ThemeResource CardStrokeColorDefault}"
                                       Padding="12,6"
                                       HorizontalAlignment="Stretch"
                                       CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8A5;" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorPrimary}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ReviewPurchaseOrderNumberText, Mode=OneWay}" 
                                                Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                   Foreground="{ThemeResource TextFillColorPrimary}">
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.PoNumber, Mode=OneWay}" 
                                        IsReadOnly="True"
                                             IsHitTestVisible="False"
                                        Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>

                            <!-- Part ID -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                       Background="{ThemeResource CardStrokeColorDefault}"
                                       Padding="12,6"
                                       HorizontalAlignment="Stretch"
                                       CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8B7;" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorPrimary}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ReviewPartIdText, Mode=OneWay}" 
                                                Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                   Foreground="{ThemeResource TextFillColorPrimary}">
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.PartID, Mode=OneWay}" 
                                        IsReadOnly="True"
                                             IsHitTestVisible="False"
                                        Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>

                            <!-- Weight/Quantity -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                    Background="{ThemeResource CardStrokeColorDefault}"
                                    Padding="12,6"
                                    HorizontalAlignment="Stretch"
                                    CornerRadius="8,8,0,0">
                                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                                <FontIcon Glyph="&#xEA8B;" 
                                             FontSize="14"
                                             Foreground="{ThemeResource TextFillColorPrimary}"/>
                                                                <TextBlock Text="{x:Bind ViewModel.ReviewWeightQuantityText, Mode=OneWay}" 
                                             Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                Foreground="{ThemeResource TextFillColorPrimary}">
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </Border>
                                                        <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.WeightQuantity, Mode=OneWay}" 
                                     IsReadOnly="True"
                                          IsHitTestVisible="False"
                                     Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>

                            <!-- Heat/Lot Number -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                       Background="{ThemeResource CardStrokeColorDefault}"
                                       Padding="12,6"
                                       HorizontalAlignment="Stretch"
                                       CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8B4;" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorPrimary}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ReviewHeatLotNumberText, Mode=OneWay}" 
                                                Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                   Foreground="{ThemeResource TextFillColorPrimary}">
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.HeatLotNumber, Mode=OneWay}" 
                                        IsReadOnly="True"
                                             IsHitTestVisible="False"
                                        Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>
                            
                            <!-- Packages Per Load -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                       Background="{ThemeResource CardStrokeColorDefault}"
                                       Padding="12,6"
                                       HorizontalAlignment="Stretch"
                                       CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE7B8;" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorPrimary}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ReviewPackagesPerLoadText, Mode=OneWay}" 
                                                Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                   Foreground="{ThemeResource TextFillColorPrimary}">
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.PackagesPerLoad, Mode=OneWay}" 
                                        IsReadOnly="True"
                                             IsHitTestVisible="False"
                                        Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>

                            <!-- Package Type -->

                            <controls:UniformGrid Columns="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Rows="2" Margin="0,0,8,0">

                                <Border Grid.Row="0" 
                                       Background="{ThemeResource CardStrokeColorDefault}"
                                       Padding="12,6"
                                       HorizontalAlignment="Stretch"
                                       CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE7B8;" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorPrimary}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ReviewPackageTypeText, Mode=OneWay}" 
                                                Style="{ThemeResource BodyStrongTextBlockStyle}"                                                   
                                                   Foreground="{ThemeResource TextFillColorPrimary}">
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <TextBox CornerRadius="0,0,8,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Text="{x:Bind ViewModel.CurrentEntry.PackageTypeName, Mode=OneWay}" 
                                        IsReadOnly="True"
                                             IsHitTestVisible="False"
                                        Background="{ThemeResource ControlFillColorDisabledBrush}"/>

                            </controls:UniformGrid>

                        </controls:UniformGrid>
                    </Border>
                </Grid>

                <!-- Navigation Bar for Single View -->
                <CommandBar Grid.Row="2" DefaultLabelPosition="Right" Margin="0,12,0,0">
                    <AppBarButton Icon="Back" 
                                 Label="{x:Bind ViewModel.ReviewPreviousLabelText, Mode=OneWay}" 
                                 Command="{x:Bind ViewModel.PreviousEntryCommand}"
                                 IsEnabled="{x:Bind ViewModel.CanGoBack, Mode=OneWay}"
                                 AutomationProperties.Name="{x:Bind ViewModel.ReviewPreviousEntryAccessibilityName, Mode=OneWay}"/>
                    
                    <AppBarButton Icon="Forward" 
                                 Label="{x:Bind ViewModel.ReviewNextLabelText, Mode=OneWay}" 
                                 Command="{x:Bind ViewModel.NextEntryCommand}"
                                 IsEnabled="{x:Bind ViewModel.CanGoNext, Mode=OneWay}"
                                 AutomationProperties.Name="{x:Bind ViewModel.ReviewNextEntryAccessibilityName, Mode=OneWay}"/>
                    
                    <AppBarSeparator/>
                    
                    <AppBarButton Icon="ViewAll" 
                                 Label="{x:Bind ViewModel.ReviewTableViewLabelText, Mode=OneWay}" 
                                 Command="{x:Bind ViewModel.SwitchToTableViewCommand}"
                                 AutomationProperties.Name="{x:Bind ViewModel.ReviewSwitchToTableAccessibilityName, Mode=OneWay}"/>
                </CommandBar>
            </Grid>

            <!-- Table View (DataGrid) -->
            <Grid Visibility="{x:Bind ViewModel.IsTableView, Mode=OneWay}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="16">
                    <controls:DataGrid x:Name="ReviewDataGrid"
                                      ItemsSource="{x:Bind ViewModel.Loads, Mode=OneWay}"
                                      AutoGenerateColumns="False"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      CanUserSortColumns="True"
                                      
                                      SelectionMode="Single"
                                      AutomationProperties.Name="{x:Bind ViewModel.ReviewEntriesTableAccessibilityName, Mode=OneWay}">
                        <controls:DataGrid.CellStyle>
                            <Style TargetType="controls:DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                            </Style>
                        </controls:DataGrid.CellStyle>
                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnLoadNumberText, Mode=OneWay}" 
                                                        Binding="{Binding LoadNumber}"
                                                        
                                                        IsReadOnly="True" 
                                                        Width="Auto"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnPartIdText, Mode=OneWay}" 
                                                        Binding="{Binding PartID}" 
                                                        Width="*"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnWeightQtyText, Mode=OneWay}" 
                                                        Binding="{Binding WeightQuantity}" 
                                                        Width="*"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnHeatLotText, Mode=OneWay}" 
                                                        Binding="{Binding HeatLotNumber}" 
                                                        Width="Auto"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnPoNumberText, Mode=OneWay}" 
                                                        Binding="{Binding PoNumber, TargetNullValue='N/A'}" 
                                                        Width="*"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnPkgsText, Mode=OneWay}" 
                                                        Binding="{Binding PackagesPerLoad}" 
                                                        Width="Auto"/>
                            <controls:DataGridTextColumn Header="{x:Bind ViewModel.ReviewColumnPkgTypeText, Mode=OneWay}" 
                                                        Binding="{Binding PackageTypeName}" 
                                                        Width="Auto"/>
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                </Border>

                <!-- Navigation Bar for Table View -->
                <CommandBar Grid.Row="1" DefaultLabelPosition="Right" Margin="0,16,0,0">
                    <AppBarButton Label="{x:Bind ViewModel.ReviewSingleViewLabelText, Mode=OneWay}" 
                                 Command="{x:Bind ViewModel.SwitchToSingleViewCommand}"
                                 AutomationProperties.Name="{x:Bind ViewModel.ReviewSwitchToSingleAccessibilityName, Mode=OneWay}">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE8A9;"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                </CommandBar>
            </Grid>
        </Grid>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" ColumnSpacing="12" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" 
                   Command="{x:Bind ViewModel.AddAnotherPartCommand}"
                   AutomationProperties.Name="{x:Bind ViewModel.ReviewAddAnotherAccessibilityName, Mode=OneWay}"
                   ToolTipService.ToolTip="{x:Bind ViewModel.GetTooltip('Button.AddAnother'), Mode=OneWay}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="{x:Bind ViewModel.ReviewAddAnotherText, Mode=OneWay}"/>
                </StackPanel>
            </Button>
            
            <Button Grid.Column="2" 
                   Command="{x:Bind ViewModel.SaveCommand}"
                   Style="{StaticResource AccentButtonStyle}"
                   AutomationProperties.Name="{x:Bind ViewModel.ReviewSaveToDatabaseAccessibilityName, Mode=OneWay}"
                   ToolTipService.ToolTip="{x:Bind ViewModel.GetTooltip('Button.SaveAndGenerate'), Mode=OneWay}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                    <TextBlock Text="{x:Bind ViewModel.ReviewSaveToDatabaseText, Mode=OneWay}"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>

