<ContentDialog
    x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_Dialog_LocationReconciliationReview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MTM_Receiving_Application.Module_Receiving.Models"
    mc:Ignorable="d"
    CloseButtonText="Close"
    DefaultButton="Close"
    Opened="Dialog_Opened"
    Title="{x:Bind ViewModel.DialogTitle, Mode=OneWay}"
>
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1240</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">1180</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">760</x:Double>
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
    </ContentDialog.Resources>

    <Grid x:Name="RootGrid" MinWidth="1120" MaxWidth="1240">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="8">
            <TextBlock FontSize="28" FontWeight="SemiBold" Text="{x:Bind ViewModel.DialogTitle, Mode=OneWay}" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.DialogDescription, Mode=OneWay}"
                TextWrapping="Wrap"
            />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,16"
            Spacing="8"
            Visibility="{x:Bind ViewModel.HasPendingItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
        >
            <ProgressBar Height="6" Maximum="100" Value="{x:Bind ViewModel.ProgressPercent, Mode=OneWay}" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock FontWeight="SemiBold" Text="Review Progress" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}"
                />
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid>
                <StackPanel
                    Spacing="16"
                    Visibility="{x:Bind ViewModel.HasPendingItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                >
                    <Border
                        Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                    >
                        <StackPanel Spacing="6">
                            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Suggested Location Update" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind ViewModel.CurrentReasonText, Mode=OneWay}"
                                TextWrapping="Wrap"
                            />
                        </StackPanel>
                    </Border>

                    <Grid ColumnSpacing="12" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Part Number" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentPartIdText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="0" Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Purchase Order" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentPurchaseOrderText, Mode=OneWay}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Original Location" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentOriginalLocationText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="New Location" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentNewLocationText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="2" Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Saved Row Quantity" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentSavedRowQuantityText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="2" Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Matched Destination Quantity" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentQuantityMovedText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="3" Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Moved By User ID" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentMovedByUserIdText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="3" Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Allocation Method" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentAllocationMethodText, Mode=OneWay}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="4" Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Moved Date" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentMovedDateText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="4" Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Moved Time" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentMovedTimeText, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border
                        Padding="14"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                    >
                        <StackPanel Spacing="4">
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Saved Row Source" />
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="{x:Bind ViewModel.CurrentDataSourceText, Mode=OneWay}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel
                    Spacing="16"
                    Visibility="{x:Bind ViewModel.IsSummaryVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                >
                    <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                        <StackPanel Spacing="6">
                            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{x:Bind ViewModel.SummaryHeadline, Mode=OneWay}" />
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.SummaryDescription, Mode=OneWay}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Grid ColumnSpacing="12" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Saved" />
                                <TextBlock FontSize="24" FontWeight="SemiBold" Text="{x:Bind ViewModel.SavedCount, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Ignored" />
                                <TextBlock FontSize="24" FontWeight="SemiBold" Text="{x:Bind ViewModel.IgnoredCount, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Unchanged" />
                                <TextBlock FontSize="24" FontWeight="SemiBold" Text="{x:Bind ViewModel.PreviewSummary.UnchangedCount, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="3" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Needs Attention" />
                                <TextBlock FontSize="24" FontWeight="SemiBold" Text="{x:Bind ViewModel.NeedsAttentionCount, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10" Visibility="{x:Bind ViewModel.HasSavedItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Spacing="12">
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.SavedSectionTitle, Mode=OneWay}" />
                            <ItemsControl ItemsSource="{x:Bind ViewModel.SavedItems, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:Model_ReceivingLocationReconciliationItem">
                                        <Border Margin="0,0,0,8" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="4">
                                                <TextBlock FontWeight="SemiBold" Text="{x:Bind PartID, Mode=OneWay}" />
                                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind PONumber, Mode=OneWay}" />
                                                <TextBlock Text="{x:Bind ExistingLocation, Mode=OneWay}" />
                                                <TextBlock Text="{x:Bind ProposedLocation, Mode=OneWay}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10" Visibility="{x:Bind ViewModel.HasIgnoredItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Spacing="12">
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.IgnoredSectionTitle, Mode=OneWay}" />
                            <ItemsControl ItemsSource="{x:Bind ViewModel.IgnoredItems, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:Model_ReceivingLocationReconciliationItem">
                                        <Border Margin="0,0,0,8" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="4">
                                                <TextBlock FontWeight="SemiBold" Text="{x:Bind PartID, Mode=OneWay}" />
                                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind PONumber, Mode=OneWay}" />
                                                <TextBlock Text="{x:Bind ExistingLocation, Mode=OneWay}" />
                                                <TextBlock Text="{x:Bind ProposedLocation, Mode=OneWay}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10" Visibility="{x:Bind ViewModel.HasUnresolvedItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Spacing="12">
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind ViewModel.UnresolvedSectionTitle, Mode=OneWay}" />
                            <ItemsControl ItemsSource="{x:Bind ViewModel.PreviewSummary.UnresolvedItems, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:Model_ReceivingLocationReconciliationItem">
                                        <Border Margin="0,0,0,8" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="4">
                                                <TextBlock FontWeight="SemiBold" Text="{x:Bind PartID, Mode=OneWay}" />
                                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Resolution, Mode=OneWay}" />
                                                <TextBlock Text="{x:Bind Details, Mode=OneWay}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <StackPanel
            Grid.Row="3"
            Margin="0,16,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="12"
            Visibility="{x:Bind ViewModel.HasPendingItems, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
        >
            <Button Command="{x:Bind ViewModel.IgnoreCurrentCommand}" Content="Ignore" MinWidth="120" />
            <Button
                MinWidth="120"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                BorderThickness="0"
                Command="{x:Bind ViewModel.SaveCurrentCommand}"
                Content="Save"
                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
            />
        </StackPanel>
    </Grid>
</ContentDialog>