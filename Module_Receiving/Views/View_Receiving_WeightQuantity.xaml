<UserControl x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_WeightQuantity" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls" 
             xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters" 
             xmlns:d="http://microsoft.com" 
             xmlns:models="using:MTM_Receiving_Application.Module_Receiving.Models" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             x:Name="Root" 
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:Converter_StringFormat x:Key="StringFormatConverter" />
        <converters:Converter_IntToVisibility x:Key="IntToVisibilityConverter" />
        <converters:Converter_DoubleToDecimal x:Key="DoubleToDecimalConverter" />
        <converters:Converter_DecimalToDouble x:Key="DecimalToDoubleConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Border Header Section -->
                <RowDefinition Height="Auto" />
                <!-- InfoBar Warning Banner Section -->
                <RowDefinition Height="*" />
                <!-- ScrollViewer consumes remaining visible area -->
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <Grid ColumnSpacing="16" RowSpacing="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Grid.Column="0" ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="Part Number:" />
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{x:Bind ViewModel.CurrentPartId, Mode=OneWay}" />
                        <TextBlock Grid.Column="3" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{x:Bind ViewModel.CurrentPartDescription, Mode=OneWay}" />
                    </Grid>

                    <Grid Grid.Column="0" Grid.Row="1" ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal" Spacing="6">
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="Loads:" />
                            <TextBlock FontSize="14" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{x:Bind ViewModel.CurrentNumberOfLoads, Mode=OneWay}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="1" Spacing="6">
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="Qty Ordered:" />
                            <TextBlock FontSize="14" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{x:Bind ViewModel.PoQuantityInfo, Mode=OneWay}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="2" Spacing="6">
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="Current Total:" />
                            <TextBlock FontSize="14" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{x:Bind ViewModel.CurrentTotal, Mode=OneWay}" />
                        </StackPanel>
                        <TextBlock x:Name="CurrentTotalReminderTextBlock" Grid.Column="4" VerticalAlignment="Center" TextWrapping="NoWrap" MaxLines="1" TextTrimming="CharacterEllipsis" Text="Remember to validate Current Total to you Packing Slip!" />
                    </Grid>

                    <Button Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Padding="16,8" VerticalAlignment="Center" Command="{x:Bind ViewModel.AutoFillCommand}" Content="{x:Bind ViewModel.WeightQuantityAutoFillText, Mode=OneWay}" FontSize="15" Style="{StaticResource AccentButtonStyle}" />
                </Grid>
            </Border>

            <!-- Warning Banner -->
            <InfoBar Grid.Row="1" IsClosable="True" IsOpen="{x:Bind ViewModel.HasWarning, Mode=OneWay}" Message="{x:Bind ViewModel.WarningMessage, Mode=OneWay}" Severity="Warning" />

            <!-- Grid-Bounded ScrollViewer -->
            <ScrollViewer Grid.Row="2" VerticalAlignment="Stretch" Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}">
                <ItemsControl x:Name="LoadsItemsControl" FlowDirection="LeftToRight" ItemsSource="{x:Bind ViewModel.Loads, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!-- ADDED: Orientation="Horizontal" prevents cards from stretching vertically to fit screen height -->
                            <controls:UniformGrid Columns="3" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_ReceivingLoad">
                            <Grid MinHeight="90" Margin="0,0,4,4" Padding="16,12" HorizontalAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Glyph="&#xE7B8;" />
                                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind LoadNumber, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Load #{0}'}" />
                                    </StackPanel>
                                </StackPanel>
                                <NumberBox Grid.Column="1" AcceptsExpression="True" AutomationProperties.Name="{Binding ElementName=Root, Path=ViewModel.WeightQuantityAccessibilityName}" Header="{Binding ElementName=Root, Path=ViewModel.WeightQuantityHeaderText}" Minimum="0" PlaceholderText="{Binding ElementName=Root, Path=ViewModel.WeightQuantityPlaceholderText}" Value="{x:Bind WeightQuantity, Mode=TwoWay, Converter={StaticResource DecimalToDoubleConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
