<UserControl
    x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_WeightQuantity"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:d="http://microsoft.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MTM_Receiving_Application.Module_Receiving.Models"
    x:Name="Root"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:Converter_StringFormat x:Key="StringFormatConverter" />
        <converters:Converter_IntToVisibility x:Key="IntToVisibilityConverter" />
        <converters:Converter_DoubleToDecimal x:Key="DoubleToDecimalConverter" />
        <converters:Converter_DecimalToDouble x:Key="DecimalToDoubleConverter" />
    </UserControl.Resources>

    <Grid x:Name="WeightQuantityRootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ReceivingWeightQuantityAdaptiveStates">
                <VisualState x:Name="ReceivingDefaultState" />
                <VisualState x:Name="ReceivingCompactState">
                    <VisualState.Setters>
                        <Setter Target="WeightQuantityRootGrid.Padding" Value="10" />
                        <Setter Target="WeightQuantityContentGrid.RowSpacing" Value="12" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ReceivingWideState">
                    <VisualState.Setters>
                        <Setter Target="WeightQuantityRootGrid.Padding" Value="20" />
                        <Setter Target="WeightQuantityContentGrid.RowSpacing" Value="20" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid
            x:Name="WeightQuantityContentGrid"
            RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--  Border Header Section  -->
                <RowDefinition Height="Auto" />
                <!--  InfoBar Warning Banner Section  -->
                <RowDefinition Height="*" />
                <!--  ScrollViewer consumes remaining visible area  -->
            </Grid.RowDefinitions>

            <!--  Header Section  -->
            <Border
                Grid.Row="0"
                Padding="16,12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <Grid ColumnSpacing="16" RowSpacing="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid
                        Grid.Row="0"
                        Grid.Column="0"
                        ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="Part Number:"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{x:Bind ViewModel.CurrentPartId, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                        <TextBlock
                            Grid.Column="3"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind ViewModel.CurrentPartDescription, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                    </Grid>

                    <Grid
                        Grid.Row="1"
                        Grid.Column="0"
                        ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="6">
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                MaxLines="1"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Loads:"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="14"
                                MaxLines="1"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="{x:Bind ViewModel.CurrentNumberOfLoads, Mode=OneWay}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                        </StackPanel>
                        <StackPanel
                            Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="6">
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                MaxLines="1"
                                Text="Current Total:"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="14"
                                MaxLines="1"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="{x:Bind ViewModel.CurrentTotal, Mode=OneWay}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                        </StackPanel>
                        <TextBlock
                            x:Name="CurrentTotalReminderTextBlock"
                            Grid.Column="3"
                            VerticalAlignment="Center"
                            MaxLines="1"
                            Text="Remember to validate Current Total to you Packing Slip!"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                    </Grid>

                    <Button
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Grid.Column="1"
                        Padding="16,8"
                        VerticalAlignment="Center"
                        Command="{x:Bind ViewModel.AutoFillCommand}"
                        Content="{x:Bind ViewModel.WeightQuantityAutoFillText, Mode=OneWay}"
                        FontSize="15"
                        Style="{StaticResource AccentButtonStyle}" />
                </Grid>
            </Border>
            <!--  Warning Banner  -->
            <InfoBar
                Grid.Row="1"
                IsClosable="True"
                IsOpen="{x:Bind ViewModel.HasWarning, Mode=OneWay}"
                Message="{x:Bind ViewModel.WarningMessage, Mode=OneWay}"
                Severity="Warning" />
            <!--  Grid-Bounded ScrollViewer  -->
            <ScrollViewer
                Grid.Row="2"
                VerticalAlignment="Stretch"
                Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}">
                <ItemsControl
                    x:Name="LoadsItemsControl"
                    FlowDirection="LeftToRight"
                    ItemsSource="{x:Bind ViewModel.Loads, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!--  ADDED: Orientation="Horizontal" prevents cards from stretching vertically to fit screen height  -->
                            <controls:UniformGrid
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Top"
                                Columns="3"
                                Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Model_ReceivingLoad">
                            <!--  Kept low padding and removed MinHeight to keep the row vertically compact  -->
                            <Grid
                                Margin="0,0,4,4"
                                Padding="16,6"
                                HorizontalAlignment="Stretch"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                                <Grid.ColumnDefinitions>
                                    <!--  Column 0: Load Title (Width adapts automatically to text size)  -->
                                    <ColumnDefinition Width="Auto" />
                                    <!--  Column 1: Sub-Label Text (Width adapts automatically with a safe margin)  -->
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!--  Column 0: Load Number Title with Icon  -->
                                <StackPanel
                                    Grid.Column="0"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal"
                                    Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE7B8;" />
                                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind LoadNumber, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Load #{0}'}" />
                                </StackPanel>
                                <NumberBox
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    AcceptsExpression="True"
                                    AutomationProperties.Name="{Binding ElementName=Root, Path=ViewModel.WeightQuantityAccessibilityName}"
                                    Minimum="0"
                                    PlaceholderText="{Binding ElementName=Root, Path=ViewModel.WeightQuantityPlaceholderText}"
                                    Value="{x:Bind WeightQuantity, Mode=TwoWay, Converter={StaticResource DecimalToDoubleConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
