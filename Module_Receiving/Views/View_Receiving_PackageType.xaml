<UserControl
    x:Class="MTM_Receiving_Application.Module_Receiving.Views.View_Receiving_PackageType"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MTM_Receiving_Application.Module_Receiving.Models"
    x:Name="Root"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:Converter_StringFormat x:Key="StringFormatConverter" />
        <converters:Converter_BooleanToVisibility x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid
        x:Name="PackageTypeRootGrid"
        Padding="20">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ReceivingPackageTypeAdaptiveStates">
                <VisualState x:Name="ReceivingDefaultState" />
                <VisualState x:Name="ReceivingCompactState">
                    <VisualState.Setters>
                        <Setter Target="PackageTypeRootGrid.Padding" Value="10" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ReceivingWideState">
                    <VisualState.Setters>
                        <Setter Target="PackageTypeRootGrid.Padding" Value="20" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Package Type Selection Bar  -->
        <Border
            Grid.Row="1"
            Margin="0,0,0,16"
            Padding="16,8"
            VerticalAlignment="Top"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid ColumnSpacing="16" RowSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Identity row: part, PO number, description  -->
                <Grid
                    Grid.Row="0"
                    Grid.ColumnSpan="3"
                    ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  Left: part number  -->
                    <StackPanel
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        Orientation="Horizontal"
                        Spacing="6">
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="Part Number:"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{x:Bind ViewModel.CurrentPartId, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                    </StackPanel>

                    <!--  Center: PO number, hidden when the workflow has none  -->
                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="6"
                        Visibility="{x:Bind ViewModel.HasPoNumber, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="PO Number:"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            MaxLines="1"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{x:Bind ViewModel.CurrentPoNumber, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                    </StackPanel>

                    <!--  Right: description  -->
                    <TextBlock
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        MaxLines="1"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind ViewModel.CurrentPartDescription, Mode=OneWay}"
                        TextAlignment="Right"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="NoWrap" />
                </Grid>

                <Border
                    Grid.Row="1"
                    Grid.ColumnSpan="3"
                    Height="1"
                    Margin="0,2"
                    Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                <StackPanel
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="8">
                    <FontIcon
                        FontSize="16"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Glyph="&#xE7B8;" />
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind ViewModel.PackageTypeHeaderText, Mode=OneWay}" />
                </StackPanel>

                <Grid
                    Grid.Row="2"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ComboBox
                        x:Name="PackageTypeComboBox"
                        Grid.Column="0"
                        MinWidth="240"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.Name="{x:Bind ViewModel.PackageTypeComboAccessibilityName, Mode=OneWay}"
                        ItemsSource="{x:Bind ViewModel.PackageTypes, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedPackageType, Mode=TwoWay}" />
                    <TextBox
                        Grid.Column="1"
                        MinWidth="240"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.Name="{x:Bind ViewModel.PackageTypeCustomAccessibilityName, Mode=OneWay}"
                        Text="{x:Bind ViewModel.CustomPackageTypeName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Visibility="{x:Bind ViewModel.IsCustomTypeVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <StackPanel
                    Grid.Row="2"
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="12">
                    <CheckBox
                        VerticalAlignment="Center"
                        Content="{x:Bind ViewModel.PackageTypeSaveAsDefaultText, Mode=OneWay}"
                        IsChecked="{x:Bind ViewModel.IsSaveAsDefault, Mode=TwoWay}" />
                    <Button
                        Padding="16,8"
                        VerticalAlignment="Center"
                        Command="{x:Bind ViewModel.AutoFillCommand}"
                        Content="{x:Bind ViewModel.PackageTypeAutoFillText, Mode=OneWay}"
                        CornerRadius="8"
                        FontSize="16"
                        Style="{StaticResource AccentButtonStyle}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.PackageTypeAutoFillTooltipText, Mode=OneWay}" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  Loads List  -->
        <ScrollViewer
            Grid.Row="2"
            VerticalAlignment="Stretch"
            HorizontalScrollBarVisibility="Disabled"
            HorizontalScrollMode="Disabled"
            Style="{StaticResource SharedScrollViewerVerticalAutoHorizontalOffHiddenStyle}"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Enabled">
            <ItemsControl
                x:Name="LoadsItemsControl"
                FlowDirection="LeftToRight"
                ItemsSource="{Binding ElementName=Root, Path=ViewModel.Loads}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!--  Mimicking your preferred uniform grid structure for precise 3-column alignment  -->
                        <controls:UniformGrid
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"
                            Columns="2"
                            Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Model_ReceivingLoad">
                        <!--  Unified layout grid inside the uniform cell template  -->
                        <Grid
                            Margin="0,0,4,8"
                            Padding="16,12"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            ColumnSpacing="6"
                            CornerRadius="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Column 0: Load Details  -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE7B8;" />
                                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding LoadDisplayText}" />
                                </StackPanel>
                            </StackPanel>

                            <!--  Column 1: Interactive NumberBox  -->
                            <NumberBox
                                Grid.Column="1"
                                AutomationProperties.Name="{Binding ElementName=Root, Path=ViewModel.PackageTypePackagesPerLoadAccessibilityName}"
                                LargeChange="5"
                                Minimum="1"
                                SmallChange="1"
                                SpinButtonPlacementMode="Hidden"
                                Value="{x:Bind PackagesPerLoad, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                            <!--  Column 2: Weight Display  -->
                            <StackPanel
                                Grid.Column="2"
                                Margin="16,0,0,0"
                                VerticalAlignment="Center">
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind WeightPerPackageLabel, Mode=OneWay}" />
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind WeightPerPackageValueDisplay, Mode=OneWay}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>



    </Grid>
</UserControl>
