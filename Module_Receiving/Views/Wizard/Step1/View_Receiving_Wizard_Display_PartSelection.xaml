<UserControl
x:Class="MTM_Receiving_Application.Module_Receiving.Views.Wizard.Step1.View_Receiving_Wizard_Display_PartSelection"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
xmlns:models="using:MTM_Receiving_Application.Module_Core.Models.InforVisual"
mc:Ignorable="d">

<UserControl.Resources>
    <converters:Converter_BooleanToVisibility x:Key="BoolToVisibilityConverter" />
</UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Part Selection DataGrid (matches Old_Module_Receiving) -->
        <Border Grid.Row="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                MinHeight="180"
                MaxHeight="280"
                Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                    <FontIcon Glyph="&#xE8FD;" 
                             FontSize="16"
                             Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Available Parts on PO" 
                              Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>
                
                <controls:DataGrid Grid.Row="1"
                                  ItemsSource="{x:Bind ViewModel.AvailablePartsOnPo, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPartFromPo, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  SelectionMode="Single">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="Part Number" 
                                                    Binding="{Binding PartNumber}" 
                                                    Width="130"/>
                        <controls:DataGridTextColumn Header="Description" 
                                                    Binding="{Binding PartDescription}" 
                                                    Width="*"/>
                        <controls:DataGridTextColumn Header="Remaining" 
                                                    Binding="{Binding RemainingQty}" 
                                                    Width="100"/>
                        <controls:DataGridTextColumn Header="Ordered" 
                                                    Binding="{Binding OrderedQty}" 
                                                    Width="90"/>
                        <controls:DataGridTextColumn Header="Line" 
                                                    Binding="{Binding PoLine}" 
                                                    Width="60"/>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </Grid>
        </Border>

        <!-- Package Type Selection (Below DataGrid) -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
            <StackPanel Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE7B8;" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Package Type (Auto-detected)" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <ComboBox Header="Type"
                              ItemsSource="{x:Bind ViewModel.AvailablePackageTypes, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedPackageType, Mode=TwoWay}"
                              Width="200"/>
                    
                    <TextBox Header="Custom Name"
                             Text="{x:Bind ViewModel.CustomPackageTypeName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Visibility="{x:Bind ViewModel.IsCustomPackageTypeVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                             Width="200"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
