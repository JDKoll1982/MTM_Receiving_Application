<UserControl
x:Class="MTM_Receiving_Application.Module_Receiving.Views.Wizard.Step1.View_Receiving_Wizard_Display_LoadCountEntry"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:converters="using:MTM_Receiving_Application.Module_Core.Converters"
mc:Ignorable="d">

<UserControl.Resources>
    <converters:Converter_BooleanToVisibility x:Key="BoolToVisibilityConverter" />
</UserControl.Resources>

    <StackPanel Spacing="12">
        <!-- Load Count Input -->
        <NumberBox
            Value="{x:Bind ViewModel.LoadCount, Mode=TwoWay}"
            Minimum="1"
            Maximum="999"
            SpinButtonPlacementMode="Inline"
            Width="180"
            Header="Number of Loads" />

        <!-- Validation Error Message -->
        <TextBlock
            Text="{x:Bind ViewModel.LoadCountValidationMessage, Mode=OneWay}"
            TextWrapping="Wrap"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Visibility="{x:Bind ViewModel.IsLoadCountValid, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}" />

        <!-- Help Text -->
        <TextBlock
            Text="Enter the number of loads to receive (1-999). Each load will require individual details in Step 2."
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            FontSize="12"
            TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
