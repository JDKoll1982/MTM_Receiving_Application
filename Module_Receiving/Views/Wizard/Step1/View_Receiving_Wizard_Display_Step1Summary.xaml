<UserControl
    x:Class="MTM_Receiving_Application.Module_Receiving.Views.Wizard.Step1.View_Receiving_Wizard_Display_Step1Summary"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- PO Number -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="PO Number:" FontWeight="SemiBold" Margin="0,0,12,8" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.PoNumberDisplay, Mode=OneWay}" Margin="0,0,0,8" />

        <!-- Part Number -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Part Number:" FontWeight="SemiBold" Margin="0,0,12,8" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.PartNumberDisplay, Mode=OneWay}" Margin="0,0,0,8" />

        <!-- Load Count -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Load Count:" FontWeight="SemiBold" Margin="0,0,12,0" />
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.LoadCountDisplay, Mode=OneWay}" />
    </Grid>
</UserControl>
