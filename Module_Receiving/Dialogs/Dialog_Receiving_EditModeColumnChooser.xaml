<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="MTM_Receiving_Application.Module_Receiving.Dialogs.Dialog_Receiving_EditModeColumnChooser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Choose visible columns"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Opened="Dialog_Opened"
    PrimaryButtonClick="Dialog_PrimaryButtonClick"
    PrimaryButtonText="Apply">
    <Grid
        x:Name="RootGrid"
        Padding="8"
        RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="6">
            <TextBlock Text="Choose which columns appear in Edit Mode. Columns are grouped to make the list easier to scan." TextWrapping="Wrap" />
            <TextBlock
                x:Name="AlwaysVisibleTextBlock"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Click="SelectAllButton_Click"
                Content="Select All" />
            <Button
                Grid.Column="1"
                Click="ClearAllButton_Click"
                Content="Clear All" />
            <Button
                Grid.Column="2"
                Click="ResetButton_Click"
                Content="Reset" />
        </Grid>

        <ScrollViewer
            Grid.Row="2"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="GroupsPanel" Spacing="16" />
        </ScrollViewer>
    </Grid>
</ContentDialog>
