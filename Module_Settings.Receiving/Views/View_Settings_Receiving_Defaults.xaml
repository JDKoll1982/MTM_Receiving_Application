<Page
    x:Class="MTM_Receiving_Application.Module_Settings.Receiving.Views.View_Settings_Receiving_Defaults"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
>
    <ScrollViewer>
        <Grid Padding="24">
            <StackPanel HorizontalAlignment="Center" Width="960" Spacing="16">
                <Border
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                >
                    <StackPanel Spacing="12">
                        <TextBlock
                            Text="Defaults"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                        />

                        <Grid ColumnSpacing="12" RowSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ComboBox
                                Grid.Row="0"
                                Grid.Column="0"
                                Header="Default Receiving Mode"
                                ToolTipService.ToolTip="This sets the starting mode the Receiving screen uses."
                                SelectedItem="{x:Bind ViewModel.DefaultReceivingMode, Mode=TwoWay}"
                            >
                                <x:String>Guided</x:String>
                                <x:String>Manual</x:String>
                                <x:String>Edit</x:String>
                            </ComboBox>

                            <TextBox
                                Grid.Row="1"
                                Grid.Column="0"
                                Header="Default Location"
                                ToolTipService.ToolTip="Applied when a receiving load advances without a location entered."
                                Text="{x:Bind ViewModel.DefaultLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            />

                            <StackPanel
                                Grid.Row="2"
                                Grid.Column="0"
                                Orientation="Horizontal"
                                Spacing="8"
                                HorizontalAlignment="Right"
                                Margin="0,16,0,0"
                            >
                                <Button
                                    Content="Save"
                                    Command="{x:Bind ViewModel.PerformSaveCommand}"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Padding="24,8"
                                />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
